# Fase 12 — Ajustar entorno QA KPI

Agregar al navegador simulado el método de fecha `toLocalDateStr()` que ya existe en el panel real.

No modificar `kpi.js`, `index.html`, `main`, Apps Script ni el panel publicado.
