name: Inventario Fase 22 — Comisiones

on:
  push:
    branches:
      - modularizacion-panel-fase22-comisiones-2026-08-06
    paths:
      - tools/inventariar-comisiones-fase22.mjs
      - INVENTARIAR_FASE22_COMISIONES.md
      - .github/workflows/inventario-fase22-comisiones.yml

permissions:
  contents: write

jobs:
  inventariar:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar rama aislada
        uses: actions/checkout@v4
        with:
          ref: modularizacion-panel-fase22-comisiones-2026-08-06
          fetch-depth: 0

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Confirmar base exacta
        run: |
          git fetch origin modularizacion-panel-fase21-operacion-agenda-2026-08-06
          git merge-base --is-ancestor origin/modularizacion-panel-fase21-operacion-agenda-2026-08-06 HEAD

      - name: Crear inventario
        run: node tools/inventariar-comisiones-fase22.mjs index.html INVENTARIO_FASE22_COMISIONES.md

      - name: Guardar inventario
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add INVENTARIO_FASE22_COMISIONES.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Inventariar bloque de comisiones"
          git pull --rebase origin modularizacion-panel-fase22-comisiones-2026-08-06
          git push origin HEAD:modularizacion-panel-fase22-comisiones-2026-08-06
