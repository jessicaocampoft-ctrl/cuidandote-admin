name: Liberar despliegue del administrador bloqueado

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/recover-stuck-pages-admin-2026-08-06.yml

permissions:
  contents: read
  pages: write

jobs:
  cancelar-despliegue-atascado:
    runs-on: ubuntu-latest
    steps:
      - name: Cancelar despliegue anterior atascado
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          code=$(curl --silent --show-error \
            --output /tmp/pages-cancel-response.txt \
            --write-out '%{http_code}' \
            --request POST \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/pages/deployments/431bc4850b42e7f7dfa4568c43dc40bf6f6782a4/cancel")
          cat /tmp/pages-cancel-response.txt || true
          echo "Código HTTP: ${code}"
          case "${code}" in
            200|202|204|404|409) exit 0 ;;
            *) exit 1 ;;
          esac
