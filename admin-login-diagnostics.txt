6265:       <button class="btn btn-ghost" onclick="closeModal('modalEditar')">Cancelar</button>
6266:       <button class="btn btn-teal" id="editSaveBtn" onclick="guardarEdicion()">Guardar cambios</button>
6267:     </div>
6268:   </div>
6269: </div>
6270: 
6271: <script>
6272: // ══════════════════════════════════════════════════════════════
6273: const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbzFlqrTerVorQDn0_u_D29rh3cTRstc1XBxYDAn5-2YyexMSpip0m2RPcjzJMK7VDjd/exec';
6274: 
6275: // ── KV SYNC — datos persistentes sincronizados en todos los dispositivos via GAS ──
6276: let _gasKV = {};
6277: let _kvDirty = {};
6278: let _kvFlushTimer = null;
6279: 
6280: async function loadAdminKV() {
6281:   try {
6282:     const r = await fetch(`${APPS_SCRIPT_URL}?action=getAdminKV&token=${encodeURIComponent(TOKEN)}`).then(x => x.json());
6283:     if (r.ok && r.kv) {
6284:       _gasKV = r.kv;
6285:       Object.entries(r.kv).forEach(([k, v]) => localStorage.setItem(k, v));
6286:     }
6287:   } catch(e) {}
6288: }
6289: 
6290: function kvGet(key) {
6291:   const v = _gasKV[key];
6292:   return (v !== undefined && v !== null) ? v : localStorage.getItem(key);
6293: }
6294: 
6295: function kvSet(key, value) {
6296:   const s = String(value);
6297:   _gasKV[key] = s;
6298:   localStorage.setItem(key, s);
6299:   _kvDirty[key] = s;
6310: }
6311: 
6312: async function _flushKV() {
6313:   _kvFlushTimer = null;
6314:   if (!Object.keys(_kvDirty).length) return;
6315:   const batch = { ..._kvDirty };
6316:   _kvDirty = {};
6317:   try {
6318:     await fetch(`${APPS_SCRIPT_URL}?action=setAdminKV&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(batch))}`);
6319:   } catch(e) {
6320:     Object.assign(_kvDirty, batch);
6321:     _kvFlushTimer = setTimeout(_flushKV, 5000);
6322:   }
6323: }
6324: 
6325: // Garantiza que leads y checklist pendientes se guarden aunque el navegador cierre
6326: window.addEventListener('beforeunload', () => {
6327:   if (!Object.keys(_kvDirty).length) return;
6328:   const batch = { ..._kvDirty };
6329:   _kvDirty = {};
6330:   fetch(`${APPS_SCRIPT_URL}?action=setAdminKV&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(batch))}`, { keepalive: true });
6331: });
6332: 
6333: let TOKEN  = sessionStorage.getItem('adminToken') || '';
6334: let currentAdminUser = JSON.parse(sessionStorage.getItem('adminUser') || 'null');
6335: let allData = {citas: [], bloqueos: [], eventos: []};
6336: let operationsData = {cuentas: [], pagos: [], config: [], historialEstados: [], plantillasPlanes: []};
6337: let teamData = {profesionales: [], asignaciones: [], novedades: [], auditoria: [], cuentas: []};
6338: let dailyOpsMode = 'manana';
6339: let dailyOpsFilter = 'todas';
6340: let dailyOpsData = {citas: [], counts: {}, horariosDisponibles: []};
6341: const DAILY_OPS_LAB_MODE = false;
6342: let PROFESSIONAL_TOKEN = sessionStorage.getItem('professionalToken') || '';
6343: let professionalSession = null;
6344: let professionalAgenda = [];
6345: let professionalMode = 'hoy';
6346: let _kpiViewMonth = null; // {m, y} o null = mes actual
6347: let _kpiServerHistory = {};
6410: function professionalName(id) {
6411:   const p = byIdFrom(teamData.profesionales, 'id', id) || byIdFrom(teamData.profesionales, 'ID', id);
6412:   return p ? (p.nombre || p.Nombre || 'Equipo') : 'Sin asignar';
6413: }
6414: 
6415: async function loadTeamData() {
6416:   if (!TOKEN) return teamData;
6417:   try {
6418:     const d = await fetch(`${APPS_SCRIPT_URL}?action=teamData&token=${encodeURIComponent(TOKEN)}`).then(r => r.json());
6419:     if (d.ok) {
6420:       teamData = {
6421:         profesionales: d.profesionales || [],
6422:         asignaciones: d.asignaciones || [],
6423:         novedades: d.novedades || [],
6424:         auditoria: d.auditoria || [],
6425:         cuentas: d.cuentas || []
6426:       };
6427:       (allData.citas || []).forEach(c => {
6428:         const a = assignmentFor(c.id);
6429:         c.profesionalId = a.ProfesionalID || '';
6430:         c.estadoAutorizacion = a.EstadoAutorizacion || '';
6431:         c.tarifaProfesional = a.Tarifa || '';
6432:       });
6433:     }
6434:   } catch(e) {
6435:     console.warn('No se pudo cargar Equipo', e);
6744:     usuario: document.getElementById('teamProUsuario').value.trim(),
6745:     email: document.getElementById('teamProEmail').value.trim(),
6746:     rol: document.getElementById('teamProRol').value,
6747:     estado: document.getElementById('teamProEstado').value,
6748:     servicios: document.getElementById('teamProServicios').value.trim(),
6749:     disponibilidad: document.getElementById('teamProDisponibilidad').value.trim(),
6750:     tarifasJSON: '{}'
6751:   };
6752:   const d = await fetch(`${APPS_SCRIPT_URL}?action=saveProfessional&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(payload))}`).then(r => r.json());
6753:   if (!d.ok) { toast(d.error || 'No se pudo guardar', 'err'); return; }
6754:   closeModal('modalProfesional');
6755:   await loadTeamData();
6756:   renderEquipo();
6757:   if (d.tempPassword) showTemporaryPassword('Contraseña temporal creada', d.tempPassword);
6758:   else toast('Profesional actualizado');
6759: }
6760: 
6761: async function resetProPassword(id) {
6762:   if (!confirm('¿Restablecer la contraseña de este usuario?')) return;
6763:   const d = await fetch(`${APPS_SCRIPT_URL}?action=resetProfessionalPassword&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}`).then(r => r.json());
6764:   if (d.ok) showTemporaryPassword('Nueva contraseña temporal', d.tempPassword);
6765:   else toast(d.error || 'No se pudo restablecer', 'err');
6766: }
6767: 
6768: async function togglePro(id, estado) {
6769:   const d = await fetch(`${APPS_SCRIPT_URL}?action=toggleProfessional&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}&estado=${encodeURIComponent(estado)}`).then(r => r.json());
6770:   if (d.ok) { await loadTeamData(); renderEquipo(); toast('Estado actualizado'); }
6771:   else toast(d.error || 'No se pudo actualizar', 'err');
6772: }
6773: 
6774: async function deletePro(id, nombre) {
6775:   const label = nombre || 'este fisioterapeuta';
6776:   if (!confirm(`¿Eliminar a ${label} de la lista de fisioterapeutas?\n\nNo podrá ingresar al portal. El historial interno se conserva para auditoría.`)) return;
6777:   const d = await fetch(`${APPS_SCRIPT_URL}?action=deleteProfessional&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}`).then(r => r.json());
6778:   if (d.ok) {
6779:     await loadTeamData();
6780:     renderEquipo();
6781:     toast('Fisioterapeuta eliminado de la lista');
6782:   } else {
6783:     toast(d.error || 'No se pudo eliminar', 'err');
6784:   }
6785: }
6786: 
6787: function abrirAsignarPro(citaId) {
6788:   const c = (allData.citas || []).find(x => String(x.id) === String(citaId));
6789:   if (!c) return toast('No encontré la cita', 'err');
6790:   const a = assignmentFor(citaId);
6791:   document.getElementById('assignCitaId').value = citaId;
6792:   document.getElementById('assignCitaResumen').innerHTML = `
6793:     <strong>${esc(c.nombre)}</strong>
6794:     <div class="team-muted">${esc(fmtDate(c.fecha))} · ${esc(c.hora)} · ${esc(c.servicio)} · Estado: ${esc(c.estado)}</div>
6832:   const params = new URLSearchParams({
6833:     action:'assignProfessional',
6834:     token:TOKEN,
6835:     citaId:document.getElementById('assignCitaId').value,
6836:     profesionalId:document.getElementById('assignProfessionalId').value,
6837:     tarifa:'',
6838:     override:document.getElementById('assignOverride').value
6839:   });
6840:   const d = await fetch(`${APPS_SCRIPT_URL}?${params.toString()}`).then(r => r.json());
6841:   if (d.ok) {
6842:     await loadTeamData();
6843:     renderEquipo();
6844:     renderAgenda(true);
6845:     if (closeOnSuccess) closeModal('modalAsignarPro');
6846:     toast('Cita asignada');
6847:     return true;
6848:   }
6849:   toast(d.error || 'No se pudo asignar', 'err');
6850:   return false;
6851: }
6852: 
6853: async function authorizeAssignPro() {
6854:   const assigned = await saveAssignPro({ closeOnSuccess:false });
6855:   if (!assigned) return;
6856:   const params = new URLSearchParams({
6857:     action:'authorizeAppointment',
6858:     token:TOKEN,
6859:     citaId:document.getElementById('assignCitaId').value,
6860:     excepcion:document.getElementById('assignExcepcion').value
6861:   });
6862:   const d = await fetch(`${APPS_SCRIPT_URL}?${params.toString()}`).then(r => r.json());
6863:   if (d.ok) {
6864:     closeModal('modalAsignarPro');
6865:     await reload();
6866:     await loadTeamData();
6867:     renderEquipo();
6868:     toast('Cita autorizada para atender');
6869:   } else toast(d.error || 'No se pudo autorizar', 'err');
6870: }
6871: 
6872: async function markPayablePaid(id) {
6873:   if (!confirm('¿Marcar esta cuenta como pagada?')) return;
6874:   const d = await fetch(`${APPS_SCRIPT_URL}?action=markPayablePaid&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}`).then(r => r.json());
6875:   if (d.ok) { await loadTeamData(); renderEquipo(); toast('Cuenta marcada como pagada'); }
6876:   else toast(d.error || 'No se pudo actualizar', 'err');
6877: }
6878: 
6879: async function loadOperationsData() {
6880:   if (!TOKEN) return operationsData;
6881:   try {
6882:     const d = await fetch(`${APPS_SCRIPT_URL}?action=operationsData&token=${encodeURIComponent(TOKEN)}`).then(r => r.json());
6883:     if (d.ok) operationsData = d;
6884:   } catch(e) {
6885:     console.warn('No se pudo cargar Pagos', e);
6886:   }
6887:   return operationsData;
6888: }
6889: 
6890: async function setupOperationsModuleUI() {
6891:   const d = await fetch(`${APPS_SCRIPT_URL}?action=setupOperationsModule&token=${encodeURIComponent(TOKEN)}`).then(r => r.json());
6892:   if (d.ok) {
6893:     await loadOperationsData();
6894:     renderPagos();
6895:     toast('Módulo de pagos inicializado');
6896:   } else toast(d.error || 'No se pudo inicializar', 'err');
6897: }
6898: 
6899: function paymentAccountLabel(id) {
6900:   const a = (operationsData.cuentas || []).find(x => String(x.ID) === String(id));
6901:   return a ? `${a.Medio} · ${a.Numero}` : (id || 'Sin cuenta');
6902: }
6903: 
6904: function paymentCandidateAppointments() {
6905:   const blocked = ['Cancelada','Cancelada a tiempo','Cancelación tardía','Reprogramada','Reserva vencida'];
6906:   return (allData.citas || [])
6907:     .filter(c => isOperationalDate(c.fecha) && !blocked.includes(c.estado))
6908:     .sort((a,b) => {
7041:     medioPago,
7042:     cuentaReceptora: accountId,
7043:     fechaPago,
7044:     comprobante: ref || 'Sin archivo adjunto',
7045:     estadoPago: 'COMPROBANTE_RECIBIDO',
7046:     observaciones: document.getElementById('payObservaciones').value.trim(),
7047:     proofFile: proofFile || null
7048:   };
7049:   const d = await fetch(APPS_SCRIPT_URL, {
7050:     method:'POST',
7051:     body:JSON.stringify({action:'savePayment', token:TOKEN, data:payload})
7052:   }).then(r => r.json());
7053:   if (d.ok) {
7054:     if (mode === 'approve') {
7055:       const obs = payload.observaciones || 'Pago confirmado desde registro de comprobante';
7056:       const v = await fetch(`${APPS_SCRIPT_URL}?action=verifyPayment&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(d.id)}&estado=PAGO_APROBADO&observaciones=${encodeURIComponent(obs)}`).then(r => r.json());
7057:       if (!v.ok) return toast(v.error || 'El comprobante se subió, pero no se pudo aprobar', 'err');
7058:       toast('Pago confirmado y cita autorizada');
7059:     } else {
7060:       toast('Comprobante subido para revisión');
7061:     }
7062:     clearPaymentForm();
7063:     await reload();
7064:     await loadOperationsData();
7065:     renderPagos();
7066:     renderAgenda(true);
7067:   } else toast(d.error || 'No se pudo registrar', 'err');
7068: }
7069: 
7070: function readPaymentProofFile() {
7071:   const input = document.getElementById('payProofFile');
7072:   const file = input?.files?.[0];
7073:   if (!file) return Promise.resolve(null);
7081:     reader.readAsDataURL(file);
7082:   });
7083: }
7084: 
7085: async function verifyPayment(id, status) {
7086:   const label = status === 'PAGO_APROBADO' ? 'aprobar este pago y autorizar la cita' : `marcar este pago como ${status}`;
7087:   if (!confirm(`¿Confirmas ${label}?`)) return;
7088:   const obs = prompt('Observación opcional para auditoría:', '') || '';
7089:   const d = await fetch(`${APPS_SCRIPT_URL}?action=verifyPayment&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}&estado=${encodeURIComponent(status)}&observaciones=${encodeURIComponent(obs)}`).then(r => r.json());
7090:   if (d.ok) {
7091:     toast(status === 'PAGO_APROBADO' ? 'Pago aprobado y cita autorizada' : 'Pago actualizado');
7092:     await reload();
7093:     await loadOperationsData();
7094:     renderPagos();
7095:   } else toast(d.error || 'No se pudo verificar', 'err');
7096: }
7097: 
7098: function renderPagos() {
7099:   fillPaymentSelectors(document.getElementById('payCitaId')?.value || '');
7100:   const pagos = operationsData.pagos || [];
7101:   const pagosUnicos = [];
7102:   const seenPayments = new Set();
7103:   pagos.forEach(p => {
7104:     const key = p.CitaID ? `cita:${p.CitaID}` : `pago:${p.ID}`;
7105:     if (seenPayments.has(key)) return;
7106:     seenPayments.add(key);
7207: }
7208: 
7209: async function doProfessionalLogin() {
7210:   const btn = document.getElementById('proLoginBtn');
7211:   const err = document.getElementById('proLoginErr');
7212:   err.style.display = 'none';
7213:   btn.disabled = true; btn.textContent = 'Verificando...';
7214:   try {
7215:     const d = await fetch(APPS_SCRIPT_URL, {
7216:       method:'POST',
7217:       body:JSON.stringify({
7218:         action:'professionalLogin',
7219:         user:document.getElementById('proUser').value.trim(),
7220:         password:document.getElementById('proPass').value
7221:       })
7222:     }).then(r => r.json());
7223:     if (!d.ok) throw new Error(d.error || 'No pudimos iniciar sesión');
7224:     PROFESSIONAL_TOKEN = d.professionalToken;
7225:     professionalSession = d.professional;
7226:     sessionStorage.setItem('professionalToken', PROFESSIONAL_TOKEN);
7227:     if (professionalSession.debeCambiarPassword) {
7228:       document.getElementById('proFirstChangeBox').style.display = 'block';
7229:       toast('Cambia la contraseña temporal para continuar');
7230:     } else {
7231:       await showProfessionalApp();
7232:     }
7236:   } finally {
7237:     btn.disabled = false; btn.textContent = 'Ingresar al portal';
7238:   }
7239: }
7240: 
7241: async function changeProfessionalPassword() {
7242:   const currentPassword = document.getElementById('proPass').value;
7243:   const newPassword = document.getElementById('proNewPass').value;
7244:   const d = await fetch(APPS_SCRIPT_URL, {
7245:     method:'POST',
7246:     body:JSON.stringify({action:'professionalChangePassword', token:PROFESSIONAL_TOKEN, currentPassword, newPassword})
7247:   }).then(r => r.json());
7248:   if (!d.ok) return toast(d.error || 'No se pudo cambiar la contraseña', 'err');
7249:   toast('Contraseña actualizada');
7250:   document.getElementById('proFirstChangeBox').style.display = 'none';
7251:   await showProfessionalApp();
7252: }
7253: 
7254: async function showProfessionalApp() {
7255:   location.hash = '/profesionales/agenda';
7256:   showOnlyScreen('proApp');
7257:   document.getElementById('proWelcome').textContent = professionalSession ? `${professionalSession.nombre} · ${professionalSession.rol}` : 'Portal del equipo';
7258:   document.getElementById('proDate').value = today();
7259:   await loadProfessionalAgenda();
7260: }
7261: 
7262: async function loadProfessionalAgenda() {
7263:   if (!PROFESSIONAL_TOKEN) return openProfessionalLoginMode();
7264:   const d = await fetch(`${APPS_SCRIPT_URL}?action=professionalAgenda&token=${encodeURIComponent(PROFESSIONAL_TOKEN)}`).then(r => r.json());
7265:   if (!d.ok) {
7266:     sessionStorage.removeItem('professionalToken');
7267:     PROFESSIONAL_TOKEN = "[REDACTED]";
7268:     toast(d.error || 'Sesión vencida', 'err');
7269:     return openProfessionalLoginMode();
7270:   }
7271:   professionalSession = d.professional;
7272:   document.getElementById('proWelcome').textContent = professionalSession ? `${professionalSession.nombre} · ${professionalSession.rol}` : 'Portal del equipo';
7273:   professionalAgenda = d.citas || [];
7274:   renderProfessionalAgenda();
7275: }
7276: 
7277: function setProfessionalMode(mode) {
7278:   professionalMode = mode;
7279:   document.querySelectorAll('.pro-seg').forEach(b => b.classList.remove('active'));
7280:   const btn = document.getElementById('proMode-' + mode);
7281:   if (btn) btn.classList.add('active');
7315:         <button class="btn btn-ghost btn-sm" onclick="openProIssue('${esc(c.id)}')">Reportar novedad</button>
7316:       </div>
7317:     </article>`;
7318:   }).join('') : '<div class="team-panel"><p class="team-muted">No hay citas asignadas para este filtro.</p></div>';
7319: }
7320: 
7321: async function markProfessionalAttended(citaId) {
7322:   if (!confirm('¿Confirmas que esta sesión ya fue atendida? Esta acción solo la puede revertir administración.')) return;
7323:   const d = await fetch(APPS_SCRIPT_URL, {
7324:     method:'POST',
7325:     body:JSON.stringify({action:'professionalMarkAttended', token:PROFESSIONAL_TOKEN, citaId})
7326:   }).then(r => r.json());
7327:   if (d.ok) { toast('Sesión marcada como atendida'); await loadProfessionalAgenda(); }
7328:   else toast(d.error || 'No se pudo marcar', 'err');
7329: }
7330: 
7331: function openProIssue(citaId) {
7332:   document.getElementById('proIssueCitaId').value = citaId;
7333:   document.getElementById('proIssueTipo').value = 'Paciente no responde';
7334:   document.getElementById('proIssueObs').value = '';
7335:   openModal('modalProIssue');
7336: }
7337: 
7338: async function sendProfessionalIssue() {
7339:   const d = await fetch(APPS_SCRIPT_URL, {
7340:     method:'POST',
7341:     body:JSON.stringify({
7342:       action:'professionalReportIssue',
7343:       token:PROFESSIONAL_TOKEN,
7344:       citaId:document.getElementById('proIssueCitaId').value,
7345:       tipo:document.getElementById('proIssueTipo').value,
7346:       observacion:document.getElementById('proIssueObs').value.trim()
7347:     })
7348:   }).then(r => r.json());
7349:   if (d.ok) { closeModal('modalProIssue'); toast('Novedad enviada a administración'); }
7350:   else toast(d.error || 'No se pudo enviar', 'err');
7351: }
7352: 
7353: function professionalSignout() {
7354:   sessionStorage.removeItem('professionalToken');
7355:   PROFESSIONAL_TOKEN = "[REDACTED]";
7356:   professionalSession = null;
7361: let _submittingBooking = false;
7362: let _submittingPatient = false;
7363: let _loginTime = null;
7364: 
7365: // ── LOGIN ──
7366: let _loginAttempts = 0;
7367: let _loginLockedUntil = 0;
7368: 
7369: async function doLogin() {
7370:   const ahora = Date.now();
7371:   if (_loginLockedUntil > ahora) {
7372:     const segs = Math.ceil((_loginLockedUntil - ahora) / 1000);
7373:     const errEl = document.getElementById('loginErr');
7374:     errEl.textContent = `Demasiados intentos. Espera ${segs} segundo${segs !== 1 ? 's' : ''}.`;
7375:     errEl.style.display = 'block';
7376:     return;
7377:   }
7378:   const pw  = document.getElementById('pwInput').value.trim();
7379:   const user = (document.getElementById('userInput')?.value || '').trim();
7380:   const btn = document.getElementById('loginBtn');
7381:   if (!pw) return;
7382:   btn.textContent = 'Verificando...'; btn.disabled = true;
7383:   try {
7384:     const r = await fetch(APPS_SCRIPT_URL, {
7385:       method: 'POST',
7386:       body: JSON.stringify({action: 'adminLogin', user, password: pw})
7387:     });
7388:     const d = await r.json();
7389:     if (d.ok) {
7390:       _loginAttempts = 0;
7391:       TOKEN = d.sessionToken;
7392:       sessionStorage.setItem('adminToken', d.sessionToken);
7393:       _loginTime = Date.now();
7394:       document.getElementById('loginScreen').style.display = 'none';
7395:       document.getElementById('adminApp').style.display   = 'block';
7396:       allData = d;
7397:       await loadAdminKV();
7398:       await loadTeamData();
7399:       await limpiarHorariosInvalidosAuto();
7400:       reloadMetas();
7401:       _initSidebarState();
7410:         errEl.textContent = 'Demasiados intentos fallidos. Acceso bloqueado por 2 minutos.';
7411:       } else {
7412:         const restantes = 5 - _loginAttempts;
7413:         errEl.textContent = `Contraseña incorrecta. Intentos restantes: ${restantes}`;
7414:       }
7415:       errEl.style.display = 'block';
7416:     }
7417:   } catch(e) {
7418:     document.getElementById('loginErr').textContent = 'Error de conexión. Revisa tu internet.';
7419:     document.getElementById('loginErr').style.display = 'block';
7420:   }
7421:   btn.textContent = 'Ingresar'; btn.disabled = false;
7422: }
7423: 
7424: function logout() {
7425:   sessionStorage.removeItem('adminToken');
7426:   location.reload();
7427: }
7428: 
7429: // ── TIMEOUT DE INACTIVIDAD (30 min) ──
7430: let _lastActivity = Date.now();
7431: const _INACTIVITY_MS = 30 * 60 * 1000;
7432: 
7433: function _resetActivity() { _lastActivity = Date.now(); }
7434: ['click','keydown','scroll','touchstart'].forEach(ev =>
7435:   document.addEventListener(ev, _resetActivity, {passive: true})
7441:   }
7442: }, 60_000);
7443: 
7444: // ── VERIFICAR SESIÓN AL VOLVER A LA PESTAÑA ──
7445: // Cubre el caso de suspensión del equipo donde JS se pausa y los timers no disparan
7446: document.addEventListener('visibilitychange', async () => {
7447:   if (document.visibilityState !== 'visible' || !TOKEN) return;
7448:   try {
7449:     const r = await fetch(`${APPS_SCRIPT_URL}?action=ping&token=${encodeURIComponent(TOKEN)}`).then(x => x.json());
7450:     if (!r.ok) {
7451:       toast('Sesión expirada. Volviendo al login...', 'warn');
7452:       setTimeout(logout, 1500);
7453:     }
7454:   } catch(e) {}
7455: });
7456: 
7457: // Auto-login si tiene sesión guardada
7458: window.addEventListener('DOMContentLoaded', async () => {
7459:   initAdminUX2026();
7460:   document.getElementById('dashDate').textContent = new Date().toLocaleDateString('es-CO',{weekday:'long',year:'numeric',month:'long',day:'numeric'});
7461:   if (location.hash.startsWith('#/profesionales') || location.hash.startsWith('#profesionales')) {
7462:     if (PROFESSIONAL_TOKEN) await showProfessionalApp();
7463:     else openProfessionalLoginMode();
7464:     return;
7465:   }
7466:   if (TOKEN) {
7467:     const btn = document.getElementById('loginBtn');
7468:     try {
7469:       const r = await fetch(APPS_SCRIPT_URL + '?action=adminData&token=' + encodeURIComponent(TOKEN));
7470:       const d = await r.json();
7471:       if (d.ok) {
7472:         _loginTime = Date.now();
7473:         document.getElementById('loginScreen').style.display = 'none';
7474:         document.getElementById('adminApp').style.display   = 'block';
7475:         allData = d;
7476:         await loadAdminKV();
7477:         await loadTeamData();
7478:         await limpiarHorariosInvalidosAuto();
7479:         reloadMetas();
7480:         _initSidebarState();
7481:         initDashboard();
7482:         await _runUrlRepairIfRequested();
7483:           return;
7484:       }
7485:     } catch(e) {}
7486:     sessionStorage.removeItem('adminToken');
7490: async function _runUrlRepairIfRequested() {
7491:   const params = new URLSearchParams(location.search);
7492:   if (params.get('repair') !== 'reschedule' || !TOKEN) return;
7493:   const nombre = params.get('nombre') || '';
7494:   const keepFecha = params.get('keepFecha') || '';
7495:   const keepHora = params.get('keepHora') || '';
7496:   if (!nombre || !keepFecha || !keepHora) return;
7497:   try {
7498:     const url = `${APPS_SCRIPT_URL}?action=repairRescheduledDuplicate&token=${encodeURIComponent(TOKEN)}&nombre=${encodeURIComponent(nombre)}&keepFecha=${encodeURIComponent(keepFecha)}&keepHora=${encodeURIComponent(keepHora)}`;
7499:     const result = await fetch(url).then(r => r.json());
7500:     document.body.dataset.repairResult = JSON.stringify(result);
7501:     if (result.ok) {
7502:       toast(result.repaired > 0 ? `Reparación lista: ${result.repaired} cita duplicada cancelada.` : 'Revisión lista: no encontré duplicados activos.', result.repaired > 0 ? 'ok' : 'warn');
7503:       await reload();
7504:       initDashboard();
7505:       renderAgenda();
7506:       renderCalendar();
7507:       renderCitasResumen();
7508:       renderIngresosDetalle();
7509:     } else {
7510:       toast('No se pudo reparar: ' + (result.error || 'respuesta inválida'), 'err');
7511:     }
7512:   } catch(e) {
7513:     document.body.dataset.repairResult = JSON.stringify({ok:false,error:e.message});
7514:     toast('Error ejecutando reparación de reprogramación', 'err');
7515:   }
7601: 
7602:   errEl.style.display = 'none';
7603:   if (!actual || !nueva || !confirmar) { errEl.textContent = 'Completa todos los campos.'; errEl.style.display = 'block'; return; }
7604:   if (nueva.length < 8) { errEl.textContent = 'La nueva contraseña debe tener al menos 8 caracteres.'; errEl.style.display = 'block'; return; }
7605:   if (nueva !== confirmar) { errEl.textContent = 'Las contraseñas nuevas no coinciden.'; errEl.style.display = 'block'; return; }
7606: 
7607:   btn.textContent = 'Guardando...'; btn.disabled = true;
7608:   try {
7609:     const r = await fetch(APPS_SCRIPT_URL, {
7610:       method: 'POST',
7611:       body: JSON.stringify({action: 'changePassword', token: TOKEN, currentPassword: actual, newPassword: nueva})
7612:     }).then(x => x.json());
7613:     if (r.ok) {
7614:       closeModal('modalCambiarPassword');
7615:       toast('Contraseña actualizada correctamente', 'ok');
7616:     } else {
7617:       errEl.textContent = r.error || 'Error al cambiar la contraseña.';
7618:       errEl.style.display = 'block';
7619:     }
7620:   } catch(e) {
7621:     errEl.textContent = 'Error de conexión. Intenta de nuevo.';
7622:     errEl.style.display = 'block';
7623:   }
7624:   btn.textContent = 'Guardar'; btn.disabled = false;
7625: }
7626: 
7627: // ── EVAL EXPRESS ──
7628: function openEvalExpress() {
7629:   // Token via sessionStorage, nunca en la URL (evita historial/logs)
7630:   sessionStorage.setItem('evalToken', TOKEN);
7631:   window.open('evaluacion-express.html', '_blank');
7632: }
7633: 
7634: // ── SIDEBAR / NAV ──
7635: function initAdminUX2026() {
7636:   const sidebar = document.getElementById('sidebar');
7637:   if (!sidebar || sidebar.dataset.uxReady) return;
7638:   sidebar.dataset.uxReady = '1';
7860:     if (saved.nps !== null) snapshot.gkKpi7 = saved.nps;
7861:     if (saved.sessions) snapshot.gkKpi6 = Math.round((saved.surveyResponses||0) / saved.sessions * 100);
7862:   }
7863:   return snapshot;
7864: }
7865: 
7866: async function loadKPIHistoryFromServer() {
7867:   try {
7868:     const d=await fetch(`${APPS_SCRIPT_URL}?action=getKPIHistory&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json());
7869:     if(d.ok){_kpiServerHistory={};(d.items||[]).forEach(x=>_kpiServerHistory[x.month]=x);}
7870:   } catch(e) {}
7871: }
7872: 
7873: function _formatKPIValue(v,type) {
7874:   if (v === null || v === undefined || Number.isNaN(v)) return 'Sin datos';
7875:   if (type === 'money') return fmtPeso(Math.round(v));
7876:   if (type === 'pct') return Math.round(v) + '%';
7877:   return Math.round(v).toLocaleString('es-CO');
7878: }
7879: 
7880: function openKPIExplorer(id, card) {
7881:   const cfg = KPI_INTERACTIVE[id]; if (!cfg) return;
7882:   _activeKPIExplorer = id;
7883:   const base = _kpiViewMonth ? new Date(_kpiViewMonth.y,_kpiViewMonth.m-1,1) : new Date();
7884:   const points=[];
7885:   for(let i=5;i>=0;i--){const d=new Date(base.getFullYear(),base.getMonth()-i,1);points.push({m:d.getMonth()+1,y:d.getFullYear(),label:d.toLocaleDateString('es-CO',{month:'short'}),value:_kpiSnapshot(d.getMonth()+1,d.getFullYear())[id]});}
8211:   const c = (dailyOpsData.citas || []).find(x => x.id === id);
8212:   if (!c) return;
8213:   if (DAILY_OPS_LAB_MODE) {
8214:     const logs = JSON.parse(localStorage.getItem('dailyOpsReminderLogs_TEST') || '[]');
8215:     logs.push({id, fecha: dailyOpsData.fecha, savedAt: new Date().toISOString(), pendientePago: c.estadoPago === 'PENDIENTE_PAGO'});
8216:     localStorage.setItem('dailyOpsReminderLogs_TEST', JSON.stringify(logs.slice(-200)));
8217:     return;
8218:   }
8219:   try { await fetch(`${APPS_SCRIPT_URL}?action=logDailyReminder&token=${encodeURIComponent(TOKEN)}&citaId=${encodeURIComponent(id)}&fecha=${encodeURIComponent(dailyOpsData.fecha)}&pendientePago=${c.estadoPago==='PENDIENTE_PAGO'}&mensaje=${encodeURIComponent(buildDailyReminderMessage(c))}`); } catch(e) {}
8220: }
8221: 
8222: async function addDailyObservationPrompt(id) {
8223:   const c = (dailyOpsData.citas || []).find(x => x.id === id);
8224:   if (!c) return;
8225:   const detalle = prompt('Observación para esta cita:', c.observaciones || '');
8226:   if (!detalle) return;
8227:   if (DAILY_OPS_LAB_MODE) {
8228:     const logs = JSON.parse(localStorage.getItem('dailyOpsObservationLogs_TEST') || '[]');
8229:     logs.push({id, fecha: dailyOpsData.fecha, detalle, savedAt: new Date().toISOString()});
8230:     localStorage.setItem('dailyOpsObservationLogs_TEST', JSON.stringify(logs.slice(-200)));
8231:     c.observaciones = detalle;
8232:     renderDailyOpsList();
8233:     toast('Modo prueba: observación guardada localmente');
8234:     return;
8235:   }
8236:   try {
8237:     const d = await fetch(`${APPS_SCRIPT_URL}?action=addDailyObservation&token=${encodeURIComponent(TOKEN)}&citaId=${encodeURIComponent(id)}&fecha=${encodeURIComponent(dailyOpsData.fecha)}&detalle=${encodeURIComponent(detalle)}`).then(r=>r.json());
8238:     toast(d.ok ? 'Observación guardada' : (d.error || 'No se pudo guardar'));
8239:     await loadDailyOps(true);
8240:   } catch(e) {
8241:     toast('No se pudo guardar la observación', 'err');
8242:   }
8243: }
8244: 
8245: function copyDailyReport() {
8246:   const c = dailyOpsData.counts || {};
8247:   if (dailyOpsMode === 'hoy') {
8248:     copyText(`Cierre del día.\n\nFecha: ${dailyOpsData.fecha}\nAtendidas: ${c.atendidas || 0}\nNo asistieron: ${c.noAsistio || 0}\nCanceladas: ${c.canceladas || 0}\nPendientes de cierre: ${c.pendientesCierre || 0}\nNovedades: ${c.novedades || 0}`);
8249:     return;
8250:   }
8251:   copyText(`Agenda de mañana revisada.\n\nFecha: ${dailyOpsData.fecha}\nCitas: ${c.total || 0}\nAutorizadas: ${c.autorizadas || 0}\nPendientes de pago: ${c.pendientesPago || 0}\nComprobantes por verificar: ${c.comprobantes || 0}\nSin fisioterapeuta: ${c.sinFisioterapeuta || 0}\nNovedades: ${c.novedades || 0}`);
8252: }
8253: 
8254: function copyDailyAvailableSlots() {
8265:       ? 'CON_PENDIENTES'
8266:       : 'LISTA';
8267:     localStorage.setItem('dailyOpsDraft_'+dailyOpsData.fecha, JSON.stringify({payload, estado, counts:dailyOpsData.counts, savedAt:new Date().toISOString()}));
8268:     toast(`Modo prueba: agenda marcada localmente como ${estado}`);
8269:     await loadDailyOps(true);
8270:     return;
8271:   }
8272:   try {
8273:     const d = await fetch(`${APPS_SCRIPT_URL}?action=saveDailyPreparation&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(payload))}`).then(r=>r.json());
8274:     toast(d.ok ? `Agenda marcada: ${d.estado || 'revisada'}` : (d.error || 'No se pudo guardar'));
8275:   } catch(e) {
8276:     localStorage.setItem('dailyOpsDraft_'+dailyOpsData.fecha, JSON.stringify({payload, counts:dailyOpsData.counts, savedAt:new Date().toISOString()}));
8277:     toast('Guardado localmente como borrador de prueba');
8278:   }
8279:   await loadDailyOps(true);
8280: }
8281: 
8282: function renderCentroAcciones() {
8283:   const list = document.getElementById('opsList');
8284:   if (!list) return;
8285:   const hoy = today();
8286:   const citas = (allData.citas || []).filter(c => !esRegistroServ(c.servicio));
8287:   const citasOperativas = citas.filter(c => isOperationalDate(c.fecha));
8288:   const citasHoy = citasOperativas.filter(c => normDate(c.fecha) === hoy && c.estado !== 'Cancelada' && c.estado !== 'Atendida');
8289:   const cobros = citasOperativas.filter(c => normDate(c.fecha) < hoy && c.estado !== 'Cancelada' && !isPagada(c.id));
8290: 
8348:   try { return JSON.parse(localStorage.getItem('adminWaitList') || '[]'); }
8349:   catch(e) { return []; }
8350: }
8351: function _saveWaitList(list) { localStorage.setItem('adminWaitList', JSON.stringify(list)); }
8352: let _waitLoaded = false;
8353: 
8354: async function _syncWaitList() {
8355:   try {
8356:     const d = await fetch(`${APPS_SCRIPT_URL}?action=getWaitlist&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json());
8357:     if (d.ok) { _saveWaitList(d.items || []); _waitLoaded = true; return true; }
8358:   } catch(e) {}
8359:   return false;
8360: }
8361: 
8362: async function addWaitPatient() {
8363:   const nombre = document.getElementById('waitNombre').value.trim();
8364:   const telefono = document.getElementById('waitTelefono').value.trim();
8365:   const servicio = document.getElementById('waitServicio').value.trim();
8366:   const preferencia = document.getElementById('waitPreferencia').value.trim();
8367:   if (!nombre || !telefono) { toast('Nombre y teléfono son obligatorios','err'); return; }
8368:   const item = {id:'w'+Date.now(),nombre,telefono,servicio,preferencia,creado:new Date().toISOString()};
8369:   try {
8370:     const d = await fetch(`${APPS_SCRIPT_URL}?action=addWaitlist&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(item))}`).then(r=>r.json());
8371:     if (!d.ok) throw new Error(d.error||'No se pudo sincronizar');
8372:     item.id = d.id || item.id;
8373:   } catch(e) { toast('Guardado localmente; se sincronizará cuando actualices el servidor','warn'); }
8374:   const list = _getWaitList(); list.unshift(item); _saveWaitList(list); _waitLoaded = true;
8375:   ['waitNombre','waitTelefono','waitServicio','waitPreferencia'].forEach(id => document.getElementById(id).value='');
8376:   renderWaitList(); toast('Paciente agregado a la lista de espera');
8377: }
8378: 
8379: async function removeWaitPatient(id) {
8380:   try { await fetch(`${APPS_SCRIPT_URL}?action=removeWaitlist&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}`).then(r=>r.json()); } catch(e) {}
8381:   _saveWaitList(_getWaitList().filter(p => p.id !== id));
8382:   renderWaitList(); toast('Paciente retirado de la lista');
8383: }
8384: 
8385: function bookWaitPatient(id) {
8386:   const p = _getWaitList().find(x => x.id === id); if (!p) return;
8387:   showView('nueva');
8388:   document.getElementById('ncName').value = p.nombre;
8389:   document.getElementById('ncPhone').value = p.telefono;
8390:   if (p.servicio) document.getElementById('ncService').value = p.servicio;
8391:   toast('Datos cargados. Completa fecha, hora y servicio.');
8392: }
8393: 
8394: async function renderWaitList(force=false) {
8395:   const el = document.getElementById('waitList'); if (!el) return;
8396:   if (!_waitLoaded || force) await _syncWaitList();
8397:   const q = _normStr(document.getElementById('waitSearch')?.value || '');
8408: }
8409: 
8410: // ── CENTRO DE AUTOMATIZACIONES ──
8411: async function loadAutomationCenter() {
8412:   const title=document.getElementById('autoStatusTitle'); if(!title)return;
8413:   title.textContent='Consultando estado...';
8414:   try {
8415:     const [status,queue]=await Promise.all([
8416:       fetch(`${APPS_SCRIPT_URL}?action=automationStatus&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json()),
8417:       fetch(`${APPS_SCRIPT_URL}?action=automationQueue&status=pending&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json())
8418:     ]);
8419:     if(!status.ok)throw new Error(status.error||'Actualiza la implementación de Apps Script');
8420:     document.getElementById('autoStatusDot').classList.toggle('on',!!status.active);
8421:     title.textContent=status.active?'Automatizaciones activas':'Programación pendiente de activar';
8422:     document.getElementById('autoStatusSub').textContent=status.active?'El sistema trabaja a las 7am, 10pm y los lunes a las 8am.':'Pulsa “Activar programación” una vez después de actualizar Apps Script.';
8423:     document.querySelectorAll('[data-auto-key]').forEach(i=>i.checked=!!status.config[i.dataset.autoKey]);
8424:     renderAutomationQueue(queue.items||[]);
8425:     renderAutomationLogs(status.logs||[]);
8426:   } catch(e) {
8427:     title.textContent='El servidor aún no tiene el motor actualizado';
8428:     document.getElementById('autoStatusSub').textContent='Copia y vuelve a implementar google-apps-script.js para activar estas funciones.';
8429:     document.getElementById('autoStatusDot').classList.remove('on');
8430:   }
8431: }
8432: 
8433: async function saveAutomationSettings() {
8434:   const config={};document.querySelectorAll('[data-auto-key]').forEach(i=>config[i.dataset.autoKey]=i.checked);
8435:   try {
8436:     const d=await fetch(`${APPS_SCRIPT_URL}?action=automationSave&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(config))}`).then(r=>r.json());
8437:     if(!d.ok)throw new Error(d.error);toast('Configuración de automatizaciones guardada');
8438:   } catch(e){toast('No se pudo guardar: actualiza Apps Script','err')}
8439: }
8440: 
8441: async function setupAutomations() {
8442:   try {
8443:     toast('Activando tareas programadas...');
8444:     const d=await fetch(`${APPS_SCRIPT_URL}?action=automationSetup&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json());
8445:     if(!d.ok)throw new Error(d.error);toast('Automatizaciones activadas');loadAutomationCenter();
8446:   } catch(e){toast('No se pudieron activar: '+(e.message||'revisa permisos'),'err')}
8447: }
8448: 
8449: async function runAutomationJob(job) {
8450:   try {
8451:     toast('Ejecutando automatización...');
8452:     const d=await fetch(`${APPS_SCRIPT_URL}?action=automationRun&job=${encodeURIComponent(job)}&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json());
8453:     if(!d.ok)throw new Error(d.error);toast('Automatización completada');loadAutomationCenter();
8454:   } catch(e){toast('Error al ejecutar: '+(e.message||'revisa el servidor'),'err')}
8455: }
8456: 
8457: function renderAutomationQueue(items) {
8458:   const el=document.getElementById('autoQueue');if(!el)return;document.getElementById('autoQueueCount').textContent=items.length;
8459:   if(!items.length){el.innerHTML='<div class="empty-compact">No hay mensajes pendientes.</div>';return;}
8460:   el.innerHTML=items.slice(0,30).map(m=>{const phone=String(m.telefono||'').replace(/\D/g,'');const url=`https://wa.me/${phone}?text=${encodeURIComponent(m.mensaje||'')}`;return `<div class="queue-item"><div class="queue-copy"><div class="queue-name">${esc(m.nombre||m.type)}</div><div class="queue-reason">${esc(m.type)} · ${esc(m.motivo||'')}</div><div class="queue-msg">${esc(m.mensaje||'')}</div></div><a class="btn btn-wa btn-sm" href="${url}" target="_blank" rel="noopener" onclick="markAutomationMessage('${esc(m.id)}')">Enviar</a></div>`}).join('');
8461: }
8462: 
8463: function renderAutomationLogs(logs) {
8464:   const el=document.getElementById('autoLogs');if(!el)return;
8465:   if(!logs.length){el.innerHTML='<div class="empty-compact">Todavía no hay ejecuciones.</div>';return;}
8466:   el.innerHTML=logs.map(l=>`<div class="auto-log"><div><strong>${esc(l.job)}</strong><div style="font-size:.68rem;color:var(--muted);margin-top:2px">${esc((l.detail||'').slice(0,90))}</div></div><span>${new Date(l.timestamp).toLocaleDateString('es-CO',{day:'numeric',month:'short'})}</span></div>`).join('');
8467: }
8468: 
8469: async function markAutomationMessage(id) {
8470:   setTimeout(async()=>{try{await fetch(`${APPS_SCRIPT_URL}?action=automationQueueDone&id=${encodeURIComponent(id)}&token=${encodeURIComponent(TOKEN)}`).then(r=>r.json());loadAutomationCenter()}catch(e){}},700);
8471: }
8472: 
8473: function toggleSidebar() {
8474:   document.getElementById('sidebar').classList.toggle('open');
8475: }
8476: 
8477: function toggleSidebarCollapse() {
8478:   const sb   = document.getElementById('sidebar');
8479:   const main = document.getElementById('mainContent');
8480:   sb.classList.toggle('sb-collapsed');
8481:   main.classList.toggle('sb-collapsed-content');
8482:   localStorage.setItem('sbCollapsed', sb.classList.contains('sb-collapsed') ? '1' : '0');
8483: }
8484: 
8485: function _initSidebarState() {
8486:   if (localStorage.getItem('sbCollapsed') === '1') {
8487:     document.getElementById('sidebar').classList.add('sb-collapsed');
8677:   _pagoIdActivo = null;
8678: }
8679: 
8680: async function confirmarPago(metodo) {
8681:   if (!_pagoIdActivo) return;
8682:   const id = _pagoIdActivo;
8683:   cerrarModalPago();
8684:   try {
8685:     const r = await fetch(`${APPS_SCRIPT_URL}?action=updatePago&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}&metodo=${encodeURIComponent(metodo)}`).then(x => x.json());
8686:     if (r.ok) {
8687:       const c = allData.citas.find(x => x.id === id);
8688:       if (c) c.pago = metodo;
8689:       if (metodo) kvSet('pago_'+id, '1');
8690:       else kvRemove('pago_'+id);
8691:       if (c) logChange(metodo ? 'Pago registrado' : 'Pago pendiente', `${c.nombre} · ${c.fecha} ${c.hora} · ${metodo || 'sin método'}`);
8692:       toast(metodo ? 'Pago registrado: ' + metodo : 'Marcado como pendiente', 'ok');
8693:       renderAgenda(); initDashboard(); renderFinanzas();
8694:     } else toast('Error al guardar: ' + (r.error || ''), 'err');
8695:   } catch(e) { toast('Error de conexión', 'err'); }
8696: }
8697: 
8698: // ── ALERTA CITA PRÓXIMA ──
8699: function checkUpcomingAlerts() {
8700:   const now      = new Date();
8701:   const todayStr = today();
8702:   const banner   = document.getElementById('upcomingAlert');
8703:   if (!banner) return;
8704: 
8705:   const candidates = allData.citas
8706:     .filter(c => c.estado !== 'Cancelada' && normDate(c.fecha) === todayStr && c.hora)
8707:     .map(c => {
8708:       const [h, m] = c.hora.split(':').map(Number);
8709:       const dt   = new Date(now.getFullYear(), now.getMonth(), now.getDate(), h, m);
8710:       const diff = (dt - now) / 60000;
8711:       return { ...c, diff };
8712:     })
9089:     };
9090:   }
9091: 
9092:   let creadas = 0, errores = 0;
9093:   for (let i = 0; i < fechas.length; i++) {
9094:     document.getElementById('ncSubmitLabel').textContent = `Creando ${i+1}/${fechas.length}...`;
9095:     const data = { ...baseData, date: fechas[i].date, time: fechas[i].time };
9096:     try {
9097:       const r = await fetch(`${APPS_SCRIPT_URL}?action=adminBook&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data))}`);
9098:       const d = await r.json();
9099:       if (d.ok) creadas++; else errores++;
9100:     } catch(e) { errores++; }
9101: 
9102:     // Segunda persona: usa la misma fecha pero la hora calculada
9103:     if (_duoActive && duoData) {
9104:       const time2  = document.getElementById('nc2Time').value;
9105:       const data2  = { ...duoData, date: fechas[i].date, time: time2 };
9106:       document.getElementById('ncSubmitLabel').textContent = `Creando turno 2/${fechas.length}...`;
9107:       try {
9108:         const r2 = await fetch(`${APPS_SCRIPT_URL}?action=adminBook&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data2))}`);
9109:         const d2 = await r2.json();
9110:         if (d2.ok) creadas++; else errores++;
9111:       } catch(e) { errores++; }
9112:     }
9113:   }
9114: 
9115:   _submittingBooking = false;
9116:   btn.disabled = false;
9117:   document.getElementById('ncSubmitLabel').textContent = origLabel;
9118: 
9119:   if (errores > 0) {
9120:     toast(`${creadas} cita${creadas!==1?'s':''} creada${creadas!==1?'s':''} · ${errores} error${errores!==1?'es':''}`, 'warn');
9121:   } else {
9122:     const personas = _duoActive ? ' (2 personas)' : '';
9123:     toast(`✓ ${creadas} cita${creadas!==1?'s':''} creada${creadas!==1?'s':''} correctamente${personas}`, 'ok');
9124:     logChange('Nueva cita', `${name}${_duoActive ? ' + '+duoData.name : ''} · ${serv} · ${creadas} turno${creadas!==1?'s':''}`);
9125:     await reload();
9148:   const cita = allData.citas.find(c => c.id === id);
9149:   if (!cita) return;
9150:   if (!validateBusinessSchedule(fecha, hora, cita.servicio, cita.modalidad, 'reagendar')) return;
9151:   const data = encodeURIComponent(JSON.stringify({
9152:     id, servicio: cita.servicio, modalidad: cita.modalidad,
9153:     fecha, hora, precio: cita.precio, notas: cita.notas || ''
9154:   }));
9155:   try {
9156:     const r = await fetch(`${APPS_SCRIPT_URL}?action=editBooking&token=${encodeURIComponent(TOKEN)}&data=${data}`);
9157:     const d = await r.json();
9158:     if (d.ok) {
9159:       logChange('Cita reagendada', `${cita.nombre} · ${cita.fecha} ${cita.hora} → ${fecha} ${hora}`);
9160:       await reload();
9161:       toast('Cita reagendada: ' + fmtDate(fecha) + ' ' + hora);
9162:       closeModal('modalDetalle');
9163:       initDashboard(); renderAgenda(); renderCalendar(); renderIngresosDetalle(); renderCitasResumen();
9164:     } else toast('Error: ' + (d.error || ''), 'err');
9165:   } catch(e) { toast('Error de conexión', 'err'); }
9166: }
9167: 
9168: // ── VISTA SEMANAL (dashboard) ──
9169: function renderWeekGrid() {
9170:   const todayStr = today();
9171:   const now = new Date();
9172:   const wd = now.getDay();
9173:   const startW = new Date(now); startW.setDate(now.getDate() - wd + (wd === 0 ? -6 : 1));
9174:   const dayNames = ['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'];
9175:   const days = Array.from({length:7}, (_,i) => {
9176:     const d = new Date(startW); d.setDate(startW.getDate() + i); return d;
9177:   });
9178:   const grid = document.getElementById('weekGrid');
9179:   if (!grid) return;
9180:   grid.innerHTML = days.map((d, i) => {
9181:     const ds = d.getFullYear() + '-' + pad(d.getMonth()+1) + '-' + pad(d.getDate());
9182:     const count = citasReales().filter(c => normDate(c.fecha) === ds).length;
9393:   t.className = 'toast-msg ' + type;
9394:   t.textContent = msg;
9395:   document.getElementById('toast').appendChild(t);
9396:   setTimeout(() => t.remove(), 3500);
9397: }
9398: 
9399: async function reload() {
9400:   try {
9401:     const r = await fetch(APPS_SCRIPT_URL + '?action=adminData&token=' + encodeURIComponent(TOKEN));
9402:     if (!r.ok) { toast('Error del servidor (' + r.status + '). Recarga la página.', 'err'); return; }
9403:     const d = await r.json();
9404:     if (d.ok) {
9405:       allData = d;
9406:       await loadTeamData();
9407:     }
9408:     else if (d.error === 'Sin permiso') { toast('Sesión expirada. Volviendo al login...', 'warn'); setTimeout(logout, 1500); }
9409:   } catch(e) {
9410:     toast('Sin conexión. Verifica tu internet.', 'err');
9411:   }
9412: }
9413: 
9414: // ── HELPERS FINANCIEROS ──
9415: const esRegistroServ = s => (s || '').toLowerCase().trim().startsWith('registro');
9416: // Detecta sesiones tipo "Full" (90 min): Completa o cualquier paquete Full
9417: const esSesionFull    = s => { const n = (s||'').toLowerCase(); return n.includes('full') || n.includes('completa'); };
9418: // Detecta sesiones tipo individual corta: Cuello/Piernas/Express/Readap/Valoración
10151: }
10152: 
10153: async function guardarNotaAdmin(id) {
10154:   const paraQuien = (document.getElementById('notaParaQuienInput').value||'').trim();
10155:   const otrasNotas = document.getElementById('notaAdminInput').value.trim();
10156:   const nota = (paraQuien ? '[PARA: ' + paraQuien + ']' + (otrasNotas ? ' ' + otrasNotas : '') : otrasNotas);
10157:   try {
10158:     const data = encodeURIComponent(JSON.stringify({id, notaAdmin: nota}));
10159:     const r = await fetch(`${APPS_SCRIPT_URL}?action=editBooking&token=${encodeURIComponent(TOKEN)}&data=${data}`);
10160:     const d = await r.json();
10161:     if (d.ok) {
10162:       const cita = allData.citas.find(c => c.id === id);
10163:       if (cita) cita.notaAdmin = nota;
10164:       toast('Nota guardada');
10165:     } else toast('Error al guardar nota', 'err');
10166:   } catch(e) { toast('Error de conexión', 'err'); }
10167: }
10168: 
10169: async function changeStatus(id, status) {
10170:   try {
10171:     const r = await fetch(`${APPS_SCRIPT_URL}?action=updateStatus&token=${encodeURIComponent(TOKEN)}&id=${id}&status=${status}`);
10172:     const d = await r.json();
10173:     if (d.ok) {
10174:       const cita = allData.citas.find(c => c.id === id);
10175:       if (cita) cita.estado = status;
10176:       if (cita) logChange('Estado cambiado', `${cita.nombre} · ${cita.servicio} ${cita.fecha} → ${status}`);
10177:       toast('Estado actualizado: ' + status);
10178:       if (status === 'No asistió' && cita) {
10179:         const t = String(cita.telefono||'').replace(/\D/g,'');
10180:         const phone = t.length <= 10 ? '57'+t : t;
10181:         if (t.length >= 7 && confirm('¿Enviarle mensaje de reagendamiento a ' + cita.nombre + '?')) {
10182:           const msg = 'Hola ' + waNombre(cita.nombre) + '! \uD83D\uDE4F Vi que no pudiste venir hoy. Espero que todo este bien. Cuando quieras reagendamos, dime que dias te quedan mejor y coordinamos. \uD83D\uDE0A — Cuidándote Fisioterapia';
10183:           window.open('https://wa.me/' + phone + '?text=' + encodeURIComponent(msg), '_blank');
10184:         }
10185:       }
10186:       initDashboard();
10187:       renderAgenda();
10188:       updateBadge();
10189:     } else toast('Error al actualizar', 'err');
10190:   } catch(e) { toast('Error de conexión', 'err'); }
10191: }
10192: 
10193: function verDetalle(id) {
10194:   const c = allData.citas.find(x => x.id === id);
10195:   if (!c) return;
10196:   const esCancelada = c.estado === 'Cancelada';
10197:   document.getElementById('modalDetalleContent').innerHTML = `
10198:     <div style="display:grid;gap:12px">
10199:       <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px">
10200:         ${row('Paciente', esc(c.nombre))} ${row('Teléfono', esc(c.telefono||'—'))}
10201:         ${row('Email', esc(c.email||'—'))} ${row('Servicio', esc(c.servicio))}
10202:         ${row('Fecha', esc(fmtDate(c.fecha) + ' · ' + c.hora))} ${row('Modalidad', esc(c.modalidad))}
10203:         ${row('Valor', esc(c.precio))} ${row('Estado', chipState(c.estado))}
10204:         ${c.direccion ? row('Dirección', esc(c.direccion)) : ''}
10205:         ${c.notas ? row('Notas del paciente', esc(c.notas), true) : ''}
10206:         ${row('ID cita', esc(c.id))}
10207:       </div>
10292:   const sel = document.querySelector('input[name="motivoCancel"]:checked');
10293:   if (!sel) { toast('Selecciona un motivo de cancelación', 'err'); return; }
10294:   const motivo = sel.value;
10295: 
10296:   const btn = document.getElementById('btnConfirmarCancelar');
10297:   btn.disabled = true; btn.textContent = 'Cancelando...';
10298: 
10299:   try {
10300:     const r = await fetch(`${APPS_SCRIPT_URL}?action=cancelBooking&token=${encodeURIComponent(TOKEN)}&id=${id}`);
10301:     const d = await r.json();
10302:     if (d.ok) {
10303:       const cita = allData.citas.find(c => c.id === id);
10304:       if (cita) { cita.estado = 'Cancelada'; cita.motivoCancelacion = motivo; }
10305:       saveCancelMotivo(id, motivo);
10306:       if (cita) logChange('Cita cancelada', `${cita.nombre} · ${cita.fecha} ${cita.hora} · ${motivo}`);
10307:       toast(esCancelExcluida(motivo) ? `🧪 Cita cancelada (${motivo} — no afecta KPIs)` : `Cita cancelada: ${motivo}`, 'ok');
10308:       document.getElementById('modalCancelar').style.display = 'none';
10309:       closeModal('modalDetalle');
10310:       initDashboard();
10311:       renderAgenda();
10312:     } else toast('Error al cancelar: ' + (d.error||''), 'err');
10313:   } catch(e) { toast('Error de conexión', 'err'); }
10314: 
10315:   btn.disabled = false; btn.textContent = 'Confirmar cancelación';
10316:   _cancelPendingId = null;
10317: }
10318: 
10319: function getCancelMotivos() {
10320:   try { return JSON.parse(kvGet('cancelMotivos') || '{}'); } catch { return {}; }
10321: }
10322: 
10323: function saveCancelMotivo(id, motivo) {
10324:   const map = getCancelMotivos();
10325:   map[id] = motivo;
10326:   kvSet('cancelMotivos', JSON.stringify(map));
10327: }
10328: 
10329: function getCancelMotivo(id) {
10330:   return getCancelMotivos()[id] || null;
10437:   const precio   = getPrecioFinalEdit();
10438:   const notas    = document.getElementById('editNotas').value.trim();
10439:   if (!fecha) { toast('Selecciona una fecha', 'err'); return; }
10440:   if (!validateBusinessSchedule(fecha, hora, servicio, modalidad, 'guardar la cita')) return;
10441:   const btn = document.getElementById('editSaveBtn');
10442:   btn.textContent = 'Guardando...'; btn.disabled = true;
10443:   try {
10444:     const data = encodeURIComponent(JSON.stringify({id, servicio, modalidad, fecha, hora, precio, notas}));
10445:     const r = await fetch(`${APPS_SCRIPT_URL}?action=editBooking&token=${encodeURIComponent(TOKEN)}&data=${data}`);
10446:     const d = await r.json();
10447:     if (d.ok) {
10448:       if (anterior) logChange('Cita editada', `${anterior.nombre} · ${anterior.fecha} ${anterior.hora} → ${fecha} ${hora} · ${servicio}`);
10449:       await reload();
10450:       toast('Cita actualizada correctamente');
10451:       closeModal('modalEditar');
10452:       initDashboard();
10453:       renderAgenda();
10454:       renderCalendar();
10455:       renderIngresosDetalle();
10456:       renderCitasResumen();
10457:     } else toast('Error al guardar: ' + (d.error||''), 'err');
10458:   } catch(e) { toast('Error de conexión', 'err'); }
10459:   btn.textContent = 'Guardar cambios'; btn.disabled = false;
10460: }
10461: 
10462: // ── Mensaje de seguimiento WhatsApp personalizado ──
10463: function seguimientoWA(tel, nombre, servicio, notaAdmin) {
10464:   const url = waLinkSeg(tel, nombre, servicio, notaAdmin);
10465:   if (!url) { toast('Sin número de teléfono registrado', 'err'); return; }
10466:   window.open(url, '_blank');
10467: }
10468: 
10469: // ── BLOQUEAR ──
10470: function renderBloqueos() {
10471:   const bl = document.getElementById('blockList');
10472:   if (!allData.bloqueos.length) {
10473:     bl.innerHTML = '<div class="empty"><p>No hay bloqueos activos</p></div>';
10474:     return;
10475:   }
10486: async function doBlock() {
10487:   const date  = document.getElementById('blDate').value;
10488:   const start = document.getElementById('blStart').value;
10489:   const end   = document.getElementById('blEnd').value;
10490:   const reason= document.getElementById('blReason').value;
10491:   if (!date||!start||!end) { toast('Completa fecha, hora inicio y fin','err'); return; }
10492:   if (start >= end) { toast('La hora de fin debe ser mayor al inicio','err'); return; }
10493:   try {
10494:     const r = await fetch(`${APPS_SCRIPT_URL}?action=block&token=${encodeURIComponent(TOKEN)}&date=${date}&startTime=${start}&endTime=${end}&reason=${encodeURIComponent(reason||'Bloqueado')}`);
10495:     const d = await r.json();
10496:     if (d.ok) {
10497:       allData.bloqueos.push({bid: d.bid||'', fecha:date, inicio:start, fin:end, motivo:reason||'Bloqueado'});
10498:       toast('Horario bloqueado correctamente');
10499:       renderBloqueos();
10500:       document.getElementById('blDate').value = '';
10501:       document.getElementById('blReason').value = '';
10502:     } else toast('Error al bloquear', 'err');
10503:   } catch(e) { toast('Error de conexión','err'); }
10504: }
10505: 
10506: async function doUnblock(bid, date, startTime) {
10507:   try {
10508:     const r = await fetch(`${APPS_SCRIPT_URL}?action=unblock&token=${encodeURIComponent(TOKEN)}&bid=${encodeURIComponent(bid)}&date=${date}&startTime=${startTime}`);
10509:     const d = await r.json();
10510:     if (d.ok) {
10511:       allData.bloqueos = allData.bloqueos.filter(b => bid ? b.bid !== bid : !(b.fecha===date && b.inicio===startTime));
10512:       toast('Bloqueo eliminado');
10513:       renderBloqueos();
10514:     } else toast('Error al eliminar: ' + (d.error||''), 'err');
10515:   } catch(e) { toast('Error de conexión','err'); }
10516: }
10517: 
10518: // ── NUEVA CITA (ADMIN) ──
10519: function toggleNcAddress() {
10520:   const m = document.getElementById('ncMod').value;
10521:   document.getElementById('ncAddrWrap').style.display = m==='Domicilio' ? 'block' : 'none';
10522: }
10523: 
10524: function onServiceMainChange() {
10525:   const val = document.getElementById('ncServiceMain').value;
10526:   const planSel = document.getElementById('ncServicePlan');
10527:   if (val === '__planes__') {
10528:     planSel.style.display = 'block';
10529:     planSel.selectedIndex = 0;
10530:     checkDomicilioWarn();
10531:   } else if (val) {
10532:     planSel.style.display = 'none';
11060:     horaInicio: hi,
11061:     horaFin:    hf,
11062:     duracion:   getDuracionStr(hi, hf),
11063:     cobro:      document.getElementById('evCobro').value.trim() || 'Sin cobro',
11064:     notas:      document.getElementById('evNotas').value.trim()
11065:   };
11066: 
11067:   try {
11068:     const r = await fetch(`${APPS_SCRIPT_URL}?action=crearEvento&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data))}`);
11069:     const d = await r.json();
11070:     if (!d.ok) { toast('Error: ' + (d.error||''), 'err'); return; }
11071:     toast('Evento creado ✓');
11072:     clearEvento();
11073:     await reload();
11074:     // Limpiar filtros para que el evento sea visible (eventos se ocultan si hay filtros activos)
11075:     document.getElementById('fSearch').value = '';
11076:     document.getElementById('fDesde').value  = '';
11077:     document.getElementById('fHasta').value  = '';
11078:     document.getElementById('fStatus').selectedIndex  = 0;
11079:     document.getElementById('fMod').selectedIndex     = 0;
11080:     document.getElementById('fService').selectedIndex = 0;
11081:     sessionStorage.removeItem('agendaFilters');
11082:     showView('agenda');
11083:   } catch(e) { toast('Error de conexión', 'err'); }
11084:   finally { btn.textContent = 'Crear evento'; btn.disabled = false; }
11085: }
11086: 
11087: function clearEvento() {
11088:   ['evTitulo','evFecha','evHoraInicio','evHoraFin','evCobro','evNotas'].forEach(id => {
11089:     const el = document.getElementById(id); if(el) el.value='';
11090:   });
11091:   document.getElementById('evTipo').selectedIndex = 0;
11092:   document.getElementById('evDuracionLabel').style.display = 'none';
11093: }
11094: 
11095: async function eliminarEvento(id) {
11096:   if (!confirm('¿Eliminar este evento?')) return;
11097:   try {
11098:     const r = await fetch(`${APPS_SCRIPT_URL}?action=eliminarEvento&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(id)}`);
11099:     const d = await r.json();
11100:     if (!d.ok) { toast('Error al eliminar', 'err'); return; }
11101:     toast('Evento eliminado');
11102:     await reload();
11103:     renderAgenda();
11104:   } catch(e) { toast('Error de conexión', 'err'); }
11105: }
11106: 
11107: // ── EDITAR EVENTO (modal) ──
11108: let _eeeId = null; // ID del evento en edición
11109: 
11110: // Normaliza cualquier string de hora a "HH:MM" (formato que acepta input[type=time])
11111: function normHHMM(t) {
11112:   if (!t) return '';
11113:   const m = String(t).match(/(\d{1,2}):(\d{2})/);
11114:   if (!m) return '';
11115:   return m[1].padStart(2,'0') + ':' + m[2];
11116: }
11117: 
11118: // Cierra el modal de edición (abre con style.display, no con clase .open)
11119: function cerrarModalEditarEvento() {
11120:   document.getElementById('modalEditarEvento').style.display = 'none';
11121: }
11182:     horaFin:    hf,
11183:     duracion:   dur,
11184:     cobro:      document.getElementById('eeeCobro').value.trim() || 'Sin cobro',
11185:     notas:      document.getElementById('eeeNotas').value.trim()
11186:   };
11187: 
11188:   try {
11189:     // Eliminar viejo → crear nuevo con los datos actualizados
11190:     const rDel = await fetch(`${APPS_SCRIPT_URL}?action=eliminarEvento&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(_eeeId)}`);
11191:     const dDel = await rDel.json();
11192:     if (!dDel.ok) { toast('Error al actualizar (eliminar): ' + (dDel.error||''), 'err'); return; }
11193: 
11194:     const rCre = await fetch(`${APPS_SCRIPT_URL}?action=crearEvento&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data))}`);
11195:     const dCre = await rCre.json();
11196:     if (!dCre.ok) { toast('Error al actualizar (crear): ' + (dCre.error||''), 'err'); return; }
11197: 
11198:     toast('Evento actualizado ✓');
11199:     cerrarModalEditarEvento();
11200:     await reload();
11201:     renderAgenda();
11202:   } catch(e) { toast('Error de conexión', 'err'); }
11203:   finally { btn.textContent = 'Guardar cambios'; btn.disabled = false; }
11204: }
11205: 
11206: async function eeeEliminar() {
11207:   if (!confirm('¿Eliminar este evento?')) return;
11208:   try {
11209:     const r = await fetch(`${APPS_SCRIPT_URL}?action=eliminarEvento&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(_eeeId)}`);
11210:     const d = await r.json();
11211:     if (!d.ok) { toast('Error al eliminar', 'err'); return; }
11212:     toast('Evento eliminado');
11213:     cerrarModalEditarEvento();
11214:     await reload();
11215:     renderAgenda();
11216:   } catch(e) { toast('Error de conexión', 'err'); }
11217: }
11218: 
11219: async function submitAdminBooking() {
11220:   if (_submittingBooking) return;
11221:   const name  = document.getElementById('ncName').value.trim();
11222:   const phone = document.getElementById('ncPhone').value.trim();
11223:   const serv  = document.getElementById('ncService').value;
11224:   const mod   = document.getElementById('ncMod').value;
11225:   const date  = document.getElementById('ncDate').value;
11226:   const time  = document.getElementById('ncTime').value;
11227:   if (!name||!serv||!date||!time) { toast('Completa los campos obligatorios (*)','err'); return; }
11228:   if (!validateBusinessSchedule(date, time, serv, mod, 'agendar')) { updateTimeHelp('ncTime','ncTimeHelp'); return; }
11229: 
11230:   _submittingBooking = true;
11231:   const btn = document.getElementById('ncSubmitBtn');
11232:   btn.textContent = 'Creando...'; btn.disabled = true;
11233: 
11268:     canal: document.getElementById('nuevaCitaCanal').value || 'Directo',
11269:     ...gimnasioData
11270:   };
11271: 
11272:   const isRecurring   = document.getElementById('ncRecurring').checked;
11273:   const totalSessions = isRecurring ? (parseInt(document.getElementById('ncSessions').value) || 1) : 1;
11274: 
11275:   try {
11276:     const r = await fetch(`${APPS_SCRIPT_URL}?action=adminBook&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data))}`);
11277:     const d = await r.json();
11278:     if (!d.ok) { toast('Error: ' + (d.error||''), 'err'); return; }
11279: 
11280:     // Crear sesiones adicionales si es recurrente
11281:     if (isRecurring && totalSessions > 1) {
11282:       btn.textContent = 'Creando sesiones...';
11283:       const baseDate = new Date(date + 'T12:00:00');
11284:       let created = 1;
11285:       let errors  = 0;
11286:       for (let s = 1; s < totalSessions; s++) {
11287:         baseDate.setDate(baseDate.getDate() + 7);
11288:         const nextData = { ...data, date: toDateStr(baseDate) };
11289:         try {
11290:           const rr = await fetch(`${APPS_SCRIPT_URL}?action=adminBook&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(nextData))}`);
11291:           const dd = await rr.json();
11292:           if (dd.ok) created++; else errors++;
11293:         } catch(ex) { errors++; }
11294:       }
11295:       if (errors > 0) toast(`${created} de ${totalSessions} sesiones creadas (${errors} fallaron)`, 'warn');
11296:       else toast(`${created} de ${totalSessions} sesiones recurrentes creadas`);
11297:       logChange('Nueva cita', `${name} · ${serv} · ${totalSessions} sesiones recurrentes desde ${date} ${time}`);
11298:     } else {
11299:       toast('Cita creada y agregada a Google Calendar');
11300:       logChange('Nueva cita', `${name} · ${serv} · ${date} ${time}`);
11301:     }
11302: 
11303:     clearNuevaCita();
11304:     await reload();
11305:     initDashboard();
11306:     // Ir a la agenda filtrada por el paciente para que la cita recién creada sea visible
11307:     document.getElementById('fSearch').value = name;
11308:     document.getElementById('fDesde').value  = '';
11309:     document.getElementById('fHasta').value  = '';
11310:     document.getElementById('fStatus').selectedIndex  = 0;
11311:     document.getElementById('fMod').selectedIndex     = 0;
11312:     document.getElementById('fService').selectedIndex = 0;
11313:     sessionStorage.removeItem('agendaFilters');
11314:     showView('agenda');
11315:   } catch(e) { toast('Error de conexión','err'); }
11316:   finally {
11317:     _submittingBooking = false;
11318:     btn.textContent = 'Crear cita'; btn.disabled = false;
11319:   }
11320: }
11321: 
11322: // ── BUSCADOR PACIENTE ──
11323: function _normStr(s) {
11324:   return (s||'').toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g,'').trim();
11325: }
11326: 
11327: function searchPatient(q) {
11328:   const dd = document.getElementById('pacDropdown');
11329:   if (!q || q.length < 2) { dd.style.display='none'; return; }
11330: 
11331:   // Clave única por persona: nombre_normalizado|teléfono (evita colisiones entre distintos pacientes)
11332:   const map = {};
11716:   const btn = document.getElementById('epSaveBtn');
11717:   btn.textContent = 'Guardando...'; btn.disabled = true;
11718:   try {
11719:     let oldNombres = [];
11720:     try { oldNombres = JSON.parse(oldNombresRaw); } catch(e) { oldNombres = [oldNombresRaw]; }
11721:     let totalActualizado = 0;
11722:     for (const oldNombre of oldNombres) {
11723:       const data = encodeURIComponent(JSON.stringify({oldNombre, newNombre, telefono, email}));
11724:       const r = await fetch(`${APPS_SCRIPT_URL}?action=editPatient&token=${encodeURIComponent(TOKEN)}&data=${data}`);
11725:       const d = await r.json();
11726:       if (d.ok) totalActualizado += d.updated || 0;
11727:     }
11728:     // Actualizar en memoria
11729:     allData.citas.forEach(c => {
11730:       if (oldNombres.includes(c.nombre)) { c.nombre = newNombre; c.telefono = telefono.replace(/\D/g,''); c.email = email; }
11731:     });
11732:     toast(`Paciente actualizado en ${totalActualizado} cita(s)`);
11733:     closeModal('modalEditarPaciente');
11734:     renderPacientes();
11735:   } catch(e) { toast('Error de conexión', 'err'); }
11736:   btn.textContent = 'Guardar cambios'; btn.disabled = false;
11737: }
11738: 
11739: async function borrarPaciente(idx) {
11740:   const p = _pacs[idx];
11741:   if (!p) return;
11742:   if (!confirm(`¿Eliminar a ${p.nombre} y sus ${p.sesiones} cita(s)?\n\nEsta acción no se puede deshacer.`)) return;
11743:   try {
11744:     // Borrar todos los nombres que usó este paciente
11745:     for (const nombre of p.nombres) {
11746:       await fetch(`${APPS_SCRIPT_URL}?action=deletePatient&token=${encodeURIComponent(TOKEN)}&nombre=${encodeURIComponent(nombre)}`);
11747:     }
11748:     allData.citas = allData.citas.filter(c => !p.nombres.includes(c.nombre));
11749:     toast(`${p.nombre} eliminado/a correctamente`);
11750:     renderPacientes();
11751:     initDashboard();
11752:   } catch(e) { toast('Error de conexión', 'err'); }
11753: }
11754: 
11755: // ── BASE DE DATOS ──
11756: let _dbPacs = [];
11757: 
11758: function initFormDB() {
11759:   // noop — form fields start empty, no defaults needed
11760: }
11761: 
11762: function dbOnServiceChange() {
11763:   const val = document.getElementById('dbServicioMain').value;
11764:   const planSel = document.getElementById('dbServicioPlan');
11765:   if (val === '__planes__') {
11766:     planSel.style.display = 'block';
11767:     document.getElementById('dbServicio').value = '';
11768:   } else {
11769:     planSel.style.display = 'none';
12004:   const btn = document.getElementById('epSaveBtn');
12005:   btn.textContent = 'Guardando...'; btn.disabled = true;
12006:   try {
12007:     let oldNombres = [];
12008:     try { oldNombres = JSON.parse(oldNombresRaw); } catch(e) { oldNombres = [oldNombresRaw]; }
12009:     let totalActualizado = 0;
12010:     for (const oldNombre of oldNombres) {
12011:       const data = encodeURIComponent(JSON.stringify({oldNombre, newNombre, telefono, email}));
12012:       const r = await fetch(`${APPS_SCRIPT_URL}?action=editPatient&token=${encodeURIComponent(TOKEN)}&data=${data}`);
12013:       const d = await r.json();
12014:       if (d.ok) totalActualizado += d.updated || 0;
12015:     }
12016:     allData.citas.forEach(c => {
12017:       if (oldNombres.includes(c.nombre)) { c.nombre = newNombre; c.telefono = telefono.replace(/\D/g, ''); c.email = email; }
12018:     });
12019:     const oldLabel = oldNombres.join(' / ');
12020:     logChange('Paciente editado', oldLabel !== newNombre ? `${oldLabel} → ${newNombre}` : `${newNombre} (datos actualizados)`);
12021:     toast(`Paciente actualizado en ${totalActualizado} cita(s)`);
12022:     closeModal('modalEditarPaciente');
12023:     renderBasedatos();
12024:   } catch(e) { toast('Error de conexión', 'err'); }
12025:   btn.textContent = 'Guardar cambios'; btn.disabled = false;
12026: }
12027: 
12028: async function dbBorrarPac(idx) {
12029:   const p = _dbPacs[idx];
12030:   if (!p) return;
12031:   if (!confirm(`¿Eliminar a ${p.nombre} y sus ${p.sesiones} cita(s)?\n\nEsta acción no se puede deshacer.`)) return;
12032:   try {
12033:     for (const nombre of p.nombres) {
12034:       const r = await fetch(`${APPS_SCRIPT_URL}?action=deletePatient&token=${encodeURIComponent(TOKEN)}&nombre=${encodeURIComponent(nombre)}`);
12035:       const d = await r.json();
12036:       if (!d.ok) { toast('Error al eliminar: ' + (d.error || 'intenta de nuevo'), 'err'); return; }
12037:     }
12038:     allData.citas    = allData.citas.filter(c => !p.nombres.includes(c.nombre));
12039:     allData.pacientes = (allData.pacientes || []).filter(p2 => !p.nombres.map(n => n.toLowerCase()).includes((p2.nombre||'').toLowerCase()));
12040:     logChange('Paciente eliminado', `${p.nombre} · ${p.sesiones} cita(s)`);
12041:     toast(`${p.nombre} eliminado/a correctamente`);
12042:     renderBasedatos();
12043:     initDashboard();
12044:   } catch(e) { toast('Error de conexión', 'err'); }
12045: }
12046: 
12047: async function agregarPacienteDB() {
12048:   if (_submittingPatient) return;
12049:   const nombre   = document.getElementById('dbNombre').value.trim();
12050:   const telefono = document.getElementById('dbTelefono').value.trim();
12051:   const email    = document.getElementById('dbEmail').value.trim();
12052:   const entidad  = document.getElementById('dbEntidad').value.trim();
12053:   const deporte  = document.getElementById('dbDeporte').value.trim();
12054:   const notas    = document.getElementById('dbNotas').value.trim();
12055: 
12056:   if (!nombre) { toast('El nombre es obligatorio', 'err'); return; }
12057:   if (telefono && telefono.replace(/\D/g, '').length < 10) {
12058:     toast('El teléfono debe tener al menos 10 dígitos', 'err');
12059:     return;
12060:   }
12061: 
12077:     priceP: 'A convenir', priceD: 'A convenir',
12078:     address: '', notes: notasFinal
12079:   };
12080: 
12081:   const esReferido = document.getElementById('dbOrigen').value === 'Referido';
12082:   const referidoPor = esReferido ? (document.getElementById('dbReferidoPor').value.trim()) : '';
12083: 
12084:   try {
12085:     const r = await fetch(`${APPS_SCRIPT_URL}?action=adminBook&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(data))}`);
12086:     const d = await r.json();
12087:     if (d.ok) {
12088:       logChange('Paciente agregado', nombre + (telefono ? ` · ${telefono}` : ''));
12089:       await reload();
12090:       renderBasedatos();
12091:       initDashboard();
12092: 
12093:       // Si es referido, generar y registrar código REF
12094:       if (esReferido) {
12095:         try {
12096:           const rg = await fetch(`${APPS_SCRIPT_URL}?action=generarCodigo&token=${encodeURIComponent(TOKEN)}&tipo=REF`);
12097:           const dg = await rg.json();
12098:           if (dg.ok) {
12099:             const codData = { codigo: dg.codigo, tipo: 'REF', paciente: nombre, telefono, referidoPor };
12100:             await fetch(`${APPS_SCRIPT_URL}?action=registrarCodigo&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(codData))}`);
12101:             // Mostrar banner con el código
12102:             document.getElementById('dbCodigoValor').textContent = dg.codigo;
12103:             document.getElementById('dbCodigoResult').style.display = 'block';
12104:             // Limpiar solo campos del paciente, dejar el banner visible
12105:             document.getElementById('dbNombre').value   = '';
12106:             document.getElementById('dbTelefono').value = '';
12107:             document.getElementById('dbEmail').value    = '';
12108:             document.getElementById('dbEntidad').value  = '';
12109:             document.getElementById('dbDeporte').value  = '';
12110:             document.getElementById('dbNotas').value    = '';
12111:             document.getElementById('dbReferidoPor').value = '';
12112:             toast(`${nombre} agregado/a · Código ${dg.codigo} generado ✓`);
12113:             await reload();
12114:           } else { toast(`${nombre} agregado/a correctamente`); limpiarFormDB(); }
12115:         } catch(ex) { toast(`${nombre} agregado/a correctamente`); limpiarFormDB(); }
12116:       } else {
12117:         toast(`${nombre} agregado/a correctamente`);
12118:         limpiarFormDB();
12119:       }
12120:     } else {
12121:       toast('Error: ' + (d.error || ''), 'err');
12122:     }
12123:   } catch(e) { toast('Error de conexión', 'err'); }
12124:   finally {
12125:     _submittingPatient = false;
12126:     btn.textContent = 'Agregar paciente'; btn.disabled = false;
12127:   }
12128: }
12129: 
12130: function limpiarFormDB() {
12131:   document.getElementById('dbNombre').value   = '';
12132:   document.getElementById('dbTelefono').value = '';
12133:   document.getElementById('dbEmail').value    = '';
12134:   document.getElementById('dbEntidad').value  = '';
12135:   document.getElementById('dbDeporte').value  = '';
12136:   document.getElementById('dbNotas').value    = '';
12137:   const rp = document.getElementById('dbReferidoPor'); if(rp) rp.value='';
12138:   const rw = document.getElementById('dbReferidoPorWrap'); if(rw) rw.style.display='none';
12139:   const rl = document.getElementById('dbReferidoList'); if(rl) rl.style.display='none';
12140:   const cr = document.getElementById('dbCodigoResult'); if(cr) cr.style.display='none';
12174:   const opts = {day:'numeric', month:'short'};
12175:   document.getElementById('calTitle').textContent =
12176:     days[0].toLocaleDateString('es-CO', opts) + ' — ' +
12177:     days[6].toLocaleDateString('es-CO', {day:'numeric', month:'short', year:'numeric'});
12178: 
12179:   // Obtener eventos personales de Google Calendar para esta semana
12180:   try {
12181:     const from = toDateStr(days[0]), to = toDateStr(days[6]);
12182:     const r = await fetch(`${APPS_SCRIPT_URL}?action=getCalEvents&token=${encodeURIComponent(TOKEN)}&from=${from}&to=${to}`);
12183:     const d = await r.json();
12184:     if (d.ok) _calGCevents = d.events || [];
12185:   } catch(e) { _calGCevents = []; }
12186: 
12187:   // Encabezado
12188:   const dayNames = ['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'];
12189:   let html = '<div class="cal-head-row"><div class="cal-hour-col"></div>';
12190:   days.forEach((d,i) => {
12191:     const ds = toDateStr(d);
12192:     const isT = ds === todayStr;
12193:     html += `<div class="cal-head-cell ${isT?'cal-today':''}">
12194:       <span class="cal-head-day">${dayNames[i]}</span>
12195:       <strong style="font-size:1rem">${d.getDate()}</strong>
12196:     </div>`;
12197:   });
12198:   html += '</div>';
12199: 
12580: }
12581: 
12582: let _remData = null;
12583: 
12584: async function cargarRecordatorios() {
12585:   const cont = document.getElementById('recContent');
12586:   cont.innerHTML = '<div class="loading-wrap"><div class="spinner"></div> Consultando base de datos...</div>';
12587:   try {
12588:     const r = await fetch(`${APPS_SCRIPT_URL}?action=getReminders&token=${encodeURIComponent(TOKEN)}`);
12589:     const d = await r.json();
12590:     if (!d.ok) { cont.innerHTML = '<div class="empty"><p>Error al cargar: ' + (d.error||'') + '</p></div>'; return; }
12591:     _remData = d;
12592:     renderRecordatorios(d);
12593:     // Actualizar badge en sidebar
12594:     const total = d.semana4.length + d.semana5.length;
12595:     const badge = document.getElementById('badgeRecordatorios');
12596:     if (badge) { badge.textContent = total; badge.style.display = total > 0 ? 'inline' : 'none'; }
12597:   } catch(e) {
12598:     cont.innerHTML = '<div class="empty"><p>Error de conexión</p></div>';
12599:   }
12600: }
12601: 
12602: function renderRecordatorios(d) {
12603:   const cont = document.getElementById('recContent');
12604:   const total = d.semana4.length + d.semana5.length;
12605: 
12606:   if (total === 0) {
12607:     cont.innerHTML = `<div class="empty" style="padding:60px 20px">
12608:       <p style="font-size:1.1rem">\u2705 Todos los pacientes están al día</p>
12609:       <p style="margin-top:8px;font-size:.85rem">No hay pacientes con más de 4 semanas sin sesión.</p>
12610:     </div>`;
12611:     return;
12612:   }
12613: 
12614:   const sinEmail4  = d.semana4.filter(p => !p.email || p.email.indexOf('@') < 0).length;
12615:   const sinEmail5  = d.semana5.filter(p => !p.email || p.email.indexOf('@') < 0).length;
12695:   // Re-render para mostrar checkmark
12696:   if (_remData) renderRecordatorios(_remData);
12697: }
12698: 
12699: async function enviarEmailsRecordatorio() {
12700:   const btn = document.getElementById('btnSendEmails');
12701:   btn.textContent = 'Enviando...'; btn.disabled = true;
12702:   try {
12703:     const r = await fetch(`${APPS_SCRIPT_URL}?action=sendReminders&token=${encodeURIComponent(TOKEN)}`);
12704:     const d = await r.json();
12705:     if (d.ok) {
12706:       toast(`\u2705 ${d.sent} email(s) enviados · ${d.skipped} sin email (envía WhatsApp manualmente)`);
12707:       // Marcar emails enviados localmente
12708:       if (_remData) {
12709:         [...(_remData.semana4||[]), ...(_remData.semana5||[])].forEach(p => {
12710:           if (p.email && p.email.indexOf('@') >= 0) kvSet('rec_email_'+p.nombre, '1');
12711:         });
12712:         renderRecordatorios(_remData);
12713:       }
12714:     } else toast('Error: ' + (d.error||''), 'err');
12715:   } catch(e) { toast('Error de conexión', 'err'); }
12716:   btn.textContent = 'Enviar emails a todos'; btn.disabled = false;
12717: }
12718: 
12719: function agendarDesdePacienteRec(encNombre, encTel, encEmail) {
12720:   const nombre   = decodeURIComponent(encNombre);
12721:   const telefono = decodeURIComponent(encTel);
12722:   const email    = decodeURIComponent(encEmail);
12723:   showView('nueva');
12724:   document.getElementById('ncName').value  = nombre;
12725:   document.getElementById('ncPhone').value = telefono;
12726:   document.getElementById('ncEmail').value = email;
12727:   document.getElementById('pacSearch').value = nombre + (telefono ? ' — ' + telefono : '');
12728:   toast('Datos de ' + nombre.split(' ')[0] + ' cargados. Completa fecha, hora y servicio.');
12729: }
12730: 
12731: // ══════════════════════════════════════════════════════════════
12732: // ── SEGUIMIENTO DE PACIENTES ──
13080: async function limpiarCitasSinHora() {
13081:   const sinHora = (allData.citas || []).filter(c => (!c.hora || isMidnightTime(c.hora)) && c.estado !== 'Cancelada');
13082:   if (sinHora.length === 0) { toast('No hay citas a medianoche/sin hora — todo está limpio ✓', 'ok'); return; }
13083:   const detalle = sinHora.map(c => `• ${c.nombre} — ${c.servicio} — ${c.fecha} — hora: ${c.hora || 'sin hora'} — estado: ${c.estado}`).join('\n');
13084:   if (!confirm(`Se encontraron ${sinHora.length} cita(s) guardadas entre 00:00 y 00:59 o sin hora:\n\n${detalle}\n\nEsto elimina esas filas de la base de datos. ¿Continuar?`)) return;
13085:   const btn = document.getElementById('btnLimpiarSinHora');
13086:   btn.textContent = 'Limpiando...'; btn.disabled = true;
13087:   try {
13088:     const r = await fetch(`${APPS_SCRIPT_URL}?action=cleanCitasSinHora&token=${encodeURIComponent(TOKEN)}`);
13089:     const d = await r.json();
13090:     if (d.ok) {
13091:       toast(`✓ ${d.deleted} cita(s) de medianoche/sin hora eliminadas correctamente`, 'ok');
13092:       await reload();
13093:     } else {
13094:       toast('Error: ' + (d.error || ''), 'err');
13095:     }
13096:   } catch(e) { toast('Error de conexión', 'err'); }
13097:   finally { btn.textContent = '🧹 Limpiar medianoche / sin hora'; btn.disabled = false; }
13098: }
13099: 
13100: async function limpiarHorariosInvalidosAuto() {
13101:   // Ya no se eliminan automáticamente citas fuera de jornada.
13102:   // Administración puede agendar horarios especiales manualmente y deben permanecer visibles.
13103:   return;
13104: }
13105: 
13106: function exportarSeguimientoCSV() {
13107:   const data  = window._segData || [];
13108:   const dataR = window._segReadapData || [];
13109:   if (!data.length && !dataR.length) { toast('No hay datos para exportar', 'err'); return; }
13110:   const header = ['Nombre','Teléfono','Email','Tipo','Servicio','Última sesión','Días','Semana/Zona','Reagendó','WA Enviado'];
13111:   const rowsD = data.map(p => [
13112:     p.nombre, p.telefono, p.email, 'Descarga muscular', p.servicio, p.fecha, p.dias,
13113:     p.semana==='sem3'?'Semana 3':p.semana==='sem4'?'Semana 4':'Semana 5+',
13166:     if (s) { s.focus(); s.select(); }
13167:   }
13168: });
13169: 
13170: // ── ENVIAR EMAIL A UN SOLO PACIENTE (recordatorio) ──
13171: async function enviarEmailUno(encNombre, semanas) {
13172:   const nombre = decodeURIComponent(encNombre);
13173:   try {
13174:     const r = await fetch(`${APPS_SCRIPT_URL}?action=sendReminders&token=${encodeURIComponent(TOKEN)}`);
13175:     const d = await r.json();
13176:     if (d.ok) {
13177:       kvSet('rec_email_'+nombre, '1');
13178:       toast('Email de recordatorio enviado a ' + nombre.split(' ')[0]);
13179:       if (_remData) renderRecordatorios(_remData);
13180:     } else toast('Error al enviar email', 'err');
13181:   } catch(e) { toast('Error de conexión', 'err'); }
13182: }
13183: 
13184: // ══════════════════════════════════════════════════════════════
13185: // ── META MENSUAL ──
13186: // ══════════════════════════════════════════════════════════════
13187: function getMeta() {
13188:   // Limpiar metaMensual si tiene valor viejo
13189:   const stored = parseInt(kvGet('metaMensual')||'0', 10);
13190:   if (stored === 8000000 || stored === 8040000) { kvSet('metaMensual', 10265000); }
13191:   return getKPIConfig().meta_ventas_mes || 10265000;
13192: }
13193: 
13194: function actualizarMetaBarra(cobrado) {
13195:   const meta = getMeta();
13196:   const fill = document.getElementById('metaBarFill');
13197:   const pct  = document.getElementById('metaPct');
13198:   const txt  = document.getElementById('metaTexto');
13310:     '\n\n¿Cancelarlas en el spreadsheet? Quedarán como Canceladas (no se borran definitivamente).';
13311:   if (!confirm(msg)) return;
13312: 
13313:   btn.disabled = true;
13314:   btn.textContent = 'Limpiando...';
13315:   let eliminadas = 0, errores = 0;
13316:   for (const dup of dups) {
13317:     try {
13318:       const r = await fetch(`${APPS_SCRIPT_URL}?action=cancelBooking&token=${encodeURIComponent(TOKEN)}&id=${encodeURIComponent(dup.id)}`).then(x => x.json());
13319:       if (r.ok) {
13320:         const cita = allData.citas.find(c => c.id === dup.id);
13321:         if (cita) cita.estado = 'Cancelada';
13322:         eliminadas++;
13323:       } else errores++;
13324:     } catch(e) { errores++; }
13325:   }
13326:   btn.disabled = false;
13327:   btn.textContent = 'Limpiar duplicados';
13328:   toast(`Duplicados corregidos: ${eliminadas}${errores ? ' · Errores: ' + errores : ''}`, eliminadas > 0 ? 'ok' : 'err');
13329:   if (eliminadas > 0) { initDashboard(); renderFinanzas(); }
13330: }
13331: 
13332: function detectarDuplicados() {
13333:   const grupos = {};
13334:   for (const c of allData.citas) {
13335:     if (c.estado === 'Cancelada' || esRegistroServ(c.servicio)) continue;
15948:   try { return JSON.parse(kvGet('encuestaStats') || '{}'); } catch { return {}; }
15949: }
15950: 
15951: async function loadEncuestaStats() {
15952:   const btn  = document.getElementById('btnCargarEncuesta');
15953:   const btn2 = document.getElementById('btnCargarEncuestaGuia');
15954:   [btn, btn2].forEach(b => { if (b) { b.disabled = true; b.textContent = '⏳ Cargando...'; } });
15955:   try {
15956:     const r = await fetch(`${APPS_SCRIPT_URL}?action=getEncuestaStats&token=${encodeURIComponent(TOKEN)}`);
15957:     const d = await r.json();
15958:     if (d.ok) {
15959:       const now = new Date();
15960:       const m = now.getMonth()+1, y = now.getFullYear();
15961:       const citasMes = citasReales().filter(c => {
15962:         const [cy,cm] = normDate(c.fecha).split('-');
15963:         return +cm===m && +cy===y;
15964:       }).length;
15965:       const encPct  = citasMes > 0 ? Math.round((d.totalMes / citasMes) * 100) : 0;
15966:       const npsVal  = (d.nps !== null && d.nps !== undefined) ? d.nps : null;
15967:       const stats   = {
15968:         nps: npsVal, encuestas: encPct,
15969:         totalRespuestas: d.totalMes, citasMes,
15970:         promotores: d.promotores, pasivos: d.pasivos, detractores: d.detractores,
15971:         fetchedAt: Date.now()
15972:       };
15973:       kvSet('encuestaStats', JSON.stringify(stats));
15977:       guardarKPIManual();
15978:       _renderEncuestaStatsUI(stats);
15979:       renderKPITablero();
15980:       renderKPIGuia();
15981:       toast(`✅ NPS: ${npsVal !== null ? npsVal : '—'} · Encuestas: ${encPct}% · ${d.totalMes} respuestas este mes`, 'ok');
15982:     } else {
15983:       toast('Error al leer el formulario: ' + (d.error || 'verifica el GAS'), 'err');
15984:     }
15985:   } catch(e) { toast('Error de conexión al cargar encuestas', 'err'); }
15986:   [btn, btn2].forEach(b => { if (b) { b.disabled = false; b.textContent = b.id === 'btnCargarEncuestaGuia' ? '🔄 Cargar encuestas' : '🔄 Cargar desde formulario'; } });
15987: }
15988: 
15989: function _renderEncuestaStatsUI(stats) {
15990:   if (!stats || (stats.nps === undefined && stats.encuestas === undefined)) return;
15991:   const npsOk     = stats.nps !== null && stats.nps !== undefined;
15992:   const colorNPS  = npsOk ? (stats.nps >= META_NPS ? 'var(--ok)' : stats.nps >= 70 ? '#f59e0b' : '#ef4444') : 'var(--muted)';
15993:   const colorEnc  = stats.encuestas >= META_ENCUESTAS ? 'var(--ok)' : '#f59e0b';
15994:   const npsEl     = document.getElementById('kpiNPSAutoTag');
15995:   const encEl     = document.getElementById('kpiEncuestasAutoTag');
15996:   if (npsEl) npsEl.innerHTML = npsOk
15997:     ? `<span style="font-size:1rem;font-weight:700;color:${colorNPS}">${stats.nps}</span>`+
15998:       ` <span style="color:var(--muted);font-size:.75rem">· ${stats.promotores}P / ${stats.pasivos}Pa / ${stats.detractores}D</span>`
15999:     : `<span style="font-size:.88rem;color:var(--muted)">Sin respuestas este mes · ${stats.totalRespuestas || 0} resp.</span>`;
16000:   if (encEl) encEl.innerHTML =
16001:     `<span style="font-size:1rem;font-weight:700;color:${colorEnc}">${stats.encuestas}%</span>`+
16002:     ` <span style="color:var(--muted);font-size:.75rem">· ${stats.totalRespuestas} resp. / ${stats.citasMes} citas</span>`;
18451:   }).join('');
18452: 
18453:   updateBonosBadge();
18454: }
18455: 
18456: async function marcarUsado(codigo) {
18457:   if (!confirm(`¿Marcar ${codigo} como Usado?\nEsto cambiará el estado en Google Sheets.`)) return;
18458:   try {
18459:     const r = await fetch(`${APPS_SCRIPT_URL}?action=actualizarCodigo&token=${encodeURIComponent(TOKEN)}&codigo=${encodeURIComponent(codigo)}&estado=Usado`);
18460:     const d = await r.json();
18461:     if (!d.ok) { toast('Error al actualizar el estado', 'err'); return; }
18462:     toast(`${codigo} marcado como Usado ✓`);
18463:     await reload();
18464:     renderCodigos();
18465:   } catch(e) { toast('Error de conexión', 'err'); }
18466: }
18467: 
18468: async function generarBono(codigoRef, referidoPor, telefonoRef) {
18469:   const bonosMes = _bonosReferidorMes(referidoPor);
18470:   if (bonosMes.length >= BONO_MAX_MES) {
18471:     toast(`${referidoPor || 'Este paciente'} ya tiene ${BONO_MAX_MES} bonos este mes — límite alcanzado`, 'err');
18472:     return;
18473:   }
18474:   if (!confirm(`¿Generar bono de $${BONO_VALOR.toLocaleString('es-CO')} para ${referidoPor || 'quien refirió'}?\nBonos este mes: ${bonosMes.length + 1}/${BONO_MAX_MES}\nSe descontará en su próxima sesión.`)) return;
18475:   try {
18476:     // Generar el número (mismo que el REF)
18477:     const numRef  = codigoRef.split('-').pop(); // ej: "001"
18478:     const mesRef  = codigoRef.split('-')[1];    // ej: "MAY"
18479:     const codBono = `BONO-${mesRef}-${numRef}`;
18480: 
18481:     const codData = {
18482:       codigo:      codBono,
18483:       tipo:        'BONO',
18484:       paciente:    referidoPor,
18485:       telefono:    telefonoRef,
18486:       referidoPor: '',
18487:       codigoRef:   codigoRef
18488:     };
18489:     const r = await fetch(`${APPS_SCRIPT_URL}?action=registrarCodigo&token=${encodeURIComponent(TOKEN)}&data=${encodeURIComponent(JSON.stringify(codData))}`);
18490:     const d = await r.json();
18491:     if (!d.ok) { toast('Error al generar bono', 'err'); return; }
18492:     toast(`Bono ${codBono} generado para ${referidoPor || 'el referidor'} ✓`);
18493:     await reload();
18494:     renderCodigos();
18495:   } catch(e) { toast('Error de conexión', 'err'); }
18496: }
18497: 
18498: 
18499: function resRow(label, val, style='') {
18500:   return `<div style="display:flex;justify-content:space-between;align-items:center;font-size:.85rem;padding:3px 0">
18501:     <span style="color:var(--muted)">${label}</span>
18502:     <span style="${style}">${val}</span>
18503:   </div>`;
18504: }
18505: 
18506: // ══════════════════════════════════════════════════════════════
18507: // ── EXPORTAR CSV ──
18508: // ══════════════════════════════════════════════════════════════
18509: function exportarCSV(modo) {
18510:   const now = new Date();
18511:   const m   = now.getMonth()+1;
18512:   const y   = now.getFullYear();
18643: }
18644: 
18645: async function generarLinkPasaporte() {
18646:   if (!_pasConfirmado) return;
18647:   const nombre = document.getElementById('pasNombreInput').value.trim();
18648:   const card   = document.getElementById('pasLinkCard');
18649:   if (!nombre) { card.style.display = 'none'; return; }
18650: 
18651:   const url = APPS_SCRIPT_URL + '?action=passportEnsure&token=' + encodeURIComponent(TOKEN)
18652:     + '&nombre=' + encodeURIComponent(nombre)
18653:     + '&telefono=' + encodeURIComponent(_pasTelefono || '');
18654:   let data;
18655:   try {
18656:     data = await fetch(url).then(function(response) { return response.json(); });
18657:   } catch (error) {
18658:     toast('No se pudo generar el pasaporte', 'error');
18659:     return;
18660:   }
18661:   if (!data.ok || !data.passport || !data.passport.link) {
18662:     toast(data.error || 'No se pudo generar el pasaporte', 'error');
18663:     return;
18664:   }
18665:   _pasCurrent = data.passport;
18666:   const link = _pasCurrent.link;
18667:   document.getElementById('pasLinkTexto').textContent = link;
18668:   card.style.display = 'block';
19244:   const slotsEl  = document.getElementById('dispSlots');
19245:   const copyWrap = document.getElementById('dispCopyWrap');
19246:   if (!fecha || !slotsEl) return;
19247: 
19248:   slotsEl.innerHTML = '<span style="color:var(--muted)">Consultando...</span>';
19249:   if (copyWrap) copyWrap.style.display = 'none';
19250: 
19251:   try {
19252:     const url = `${APPS_SCRIPT_URL}?action=availability&date=${encodeURIComponent(fecha)}&service=${encodeURIComponent(servicio||'')}`;
19253:     const res  = await fetch(url).then(r => r.json());
19254:     if (!res.ok) throw new Error(res.error || 'Error');
19255: 
19256:     const slots = res.slots || {};
19257:     const libres = DISP_SLOTS_ORDER.filter(s => slots[s]);
19258:     const html = DISP_SLOTS_ORDER.map(s => {
19259:       const libre = !!slots[s];
19260:       const h = parseInt(s.split(':')[0]);
19261:       const label = h < 12 ? s + ' AM' : (h === 12 ? '12:00 PM' : (h-12) + ':00 PM');
19262:       return `<div class="disp-slot ${libre ? 'libre' : 'ocupado'}">${libre ? '✓ ' : ''}${label}</div>`;
19263:     }).join('');
19264: 
19265:     slotsEl.innerHTML = libres.length
19266:       ? `<div class="disp-slots">${html}</div>`
19267:       : `<div class="disp-slots">${html}</div><p style="text-align:center;color:var(--muted);font-size:.85rem;margin:0">Sin horarios disponibles este día</p>`;
19268: 
19269:     if (copyWrap) copyWrap.style.display = libres.length ? 'block' : 'none';
19754: // ── Carga pacientes inactivos 3+ meses desde GAS ──
19755: async function cargarInactivos() {
19756:   const panel = document.getElementById('recInactivosPanel');
19757:   const btn   = document.getElementById('recBtnCargar');
19758:   if (!panel) return;
19759:   panel.innerHTML = '<div style="text-align:center;padding:30px"><div class="spinner"></div><div style="margin-top:10px;color:var(--muted);font-size:.83rem">Consultando base de datos...</div></div>';
19760:   if (btn) { btn.disabled = true; btn.style.opacity = '0.6'; }
19761:   try {
19762:     const r = await fetch(`${APPS_SCRIPT_URL}?action=getInactivos&token=${encodeURIComponent(TOKEN)}`);
19763:     const d = await r.json();
19764:     if (!d.ok) { panel.innerHTML = `<div style="color:var(--err,#ef4444);padding:20px">Error: ${d.error||'Sin respuesta del servidor'}</div>`; return; }
19765:     renderInactivos(d.inactivos || []);
19766:   } catch(e) {
19767:     panel.innerHTML = '<div style="color:var(--err,#ef4444);padding:20px">Error de conexión. Verifica que el GAS esté actualizado y vuelve a intentarlo.</div>';
19768:   } finally {
19769:     if (btn) { btn.disabled = false; btn.style.opacity = '1'; }
19770:   }
19771: }
19772: 
19773: let _recCurrentLista = [];
19774: let _recMsgTipo = 0;
19775: 
19776: function _recPreguntaDolencia(servicio) {
19777:   const s = (servicio || '').toLowerCase();
19778:   if (s.includes('cuello') || s.includes('espalda'))  return '¿cómo sigues del cuello y la espalda?';
19779:   if (s.includes('pierna'))                            return '¿cómo sigues de las piernas?';
19780:   if (s.includes('completa'))                          return '¿cómo sigues con la tensión muscular?';
19781:   if (s.includes('valorac'))                           return '¿cómo sigues con tu proceso?';
19782:   if (s.includes('readapt'))                           return '¿cómo sigues con tu readaptación?';
19783:   if (s.includes('recuper') || s.includes('full'))     return '¿cómo sigues con tu recuperación?';
19784:   return '¿cómo sigues de salud?';
